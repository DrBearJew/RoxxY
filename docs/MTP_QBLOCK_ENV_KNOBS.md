# MTP / QBlock env knob registry

Last updated: 2026-06-23

Purpose: stop QBlock/PDMQ/WMMA env knob drift. If a new env variable affects QBlock, PDMQ, packed8/packed16 QBlock routing, WMMA probes, or strict verification, add it here and to `scripts/hip/qblock-env-audit.py` in the same patch.

## PDMQ K sibling selector

`GGML_CUDA_ROCM_PDMQ_K_CACHE=0|1` controls the compressed-K sidecar allocation. The legacy alias `GGML_CUDA_ROCM_Q8K_DOT4_PACKED16_K_CACHE=0|1` is still honored.

`GGML_CUDA_ROCM_PDMQ_K_FORMAT=default|packed16_q8|packed8_q4|packed4_q4|packed4|packed4_q2|none`. Default remains `packed16_q8` for normal no-`--cache-type-k` PDMQ/canonical runs. When the user requests `--cache-type-k q8_0` or `--cache-type-k q4_0`, the default PDMQ format becomes `packed8_q4`; these CLI spellings select the compact q4 replacement path, not a legacy raw K-cache tensor. `packed4`, `packed4_q4`, and `packed4_q4_144` are compatibility aliases for the same 144B q4 storage as `packed8_q4`; they do not select the reserved 80B q2 format. Explicit `packed4_q2` remains unsupported/fail-closed because there is no native int2 compute path. Forced route mode must abort on missing or wrong metadata.

Packed8 q4 is a lossy KV format. It is a perf/VRAM route, not a canonical token-hash route. Daily-tree `q8_0` K controls also miss the current packed16 canonical hash, so do not spend promotion time chasing `accabc9a` with global q4 scale tweaks.

## Packed16 standard and packed8 q8-request K contracts

Packed16 K is the standard/canonical persistent q8-like PDMQ storage, not f16-class VRAM. Packed8 K is the replacement selected for q8_0 K requests.

For the active QBlock head dim `D=256`, packed8 q4 uses this layout. The `packed4`/`packed4_q4` aliases intentionally use the same bytes:

```text
payload = (256 / 8) I32 = 32 * 4 = 128 bytes
scales  = (256 / 32) F16 = 8 * 2 = 16 bytes
total   = 144 bytes per K row per KV head
```

Packed16 fallback uses:

```text
payload = (256 / 4) I32 = 64 * 4 = 256 bytes
scales  = (256 / 32) F16 = 8 * 2 = 16 bytes
total   = 272 bytes per K row per KV head
```

That is q8_0-equivalent footprint (`8 * 34` bytes per row/head), versus f16 K at `256 * 2 = 512` bytes per row/head. Say “34 bytes” only when explicitly referring to one 32-dim block, not a full D=256 row.

## Packed16 DBV paged-attention full-map authority

| Env | Default | Status | Scope | Notes |
| --- | --- | --- | --- | --- |
| `GGML_CUDA_ROCM_PACKED16_DBV_PAGED_ATTENTION` | `0` | guarded candidate | packed16 DBV FA | Enables packed16 DBV FA page-table consumption for full current-K logical-to-physical maps. It is explicit-only; no promotion without strict hash/draft and speed gates. |
| `GGML_CUDA_ROCM_PACKED16_DBV_PAGED_ATTENTION_REQUIRE_FULL_MAP` | `1` | correctness guard | packed16 DBV FA | When DBV paged attention is requested, missing/incomplete full current-K maps fail closed with `requires_full_current_k_page_table` instead of silently falling back to identity. Set `0` only for legacy fallback archaeology. |
| `GGML_CUDA_ROCM_PACKED16_DBV_PAGED_ATTENTION_TRACE` | `0` | diagnostic | packed16 DBV FA | Emits `PWMMA DBV PAGED_ATTENTION` and skip/reject context. Diagnostic only. |
| `GGML_CUDA_ROCM_PACKED16_DBV_PAGED_ATTENTION_MIN_NQ` | `16` | route threshold | packed16 DBV FA | Minimum `nq` for the DBV paged-attention candidate. Lower only for bounded routeproofs. |
| `GGML_CUDA_ROCM_PACKED16_DBV_PAGED_ATTENTION_SUPPRESS_FULL_MAP_PUBLISH` | `0` | destructive diagnostic | host KV map publisher | Host-side negative-test hook. With DBV paged attention still requested in FA, suppresses full-map publication so `REQUIRE_FULL_MAP=1` must fail closed. Never use for speed or production. |

## Persistent V4_K16D16 V cache candidate

Default-on, HIP-only standard storage for the persistent V144/PV4 cache stack when it is compiled. The 130 B format is lower-VRAM but shared-scale and not q4_0-exact; the 144 B format has no VRAM savings and exists to persist the oracle-proven q4_0-exact K16D16 layout.

V144/PV4 is the standard scalar path. `GGML_CUDA_ROCM_V4_K16D16_144_PROFILE` is a separate explicit DP16-DOT/profile switch and must not be treated as the standard V144/PV4 activation knob. The legacy compatibility knob `GGML_CUDA_ROCM_V4_K16D16_144_PV4=0|1` controls the standard scalar V144/PV4 path; unset means enabled. Use `GGML_CUDA_ROCM_V4_K16D16_144_PV4_DISABLE=1` as a hard runtime rollback when a control run needs V144/PV4 inactive.

Fast active-RS route gates now default to batched target verification; set `LLAMA_MTP_TARGET_BATCH_VERIFY_UNSAFE=0` for strict serial controls. Explicit opt-outs remain available for rollback: `GGML_CUDA_ROCM_V4_K16D16_144_V_CACHE=0`, `GGML_CUDA_ROCM_V4_K16D16_144_MTP_DRAFT_V_CACHE=0`, `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_DECODE_STAGE_AUTO=0`, and `GGML_CUDA_ROCM_V4_K16D16_144_PDMQ_DECODE_SPLITK=1`. DP16-DOT/profile-only controls such as `GGML_CUDA_DP16_FA_QPACK` stay explicit unless the profile is deliberately enabled.

| Env | Default | Status | Scope | Notes |
| --- | --- | --- | --- | --- |
| `GGML_CUDA_ROCM_V4_K16D16_V_CACHE` | `0` | candidate | KV cache / CUDA pack op | Allocates experimental internal `GGML_TYPE_V4_K16D16` V storage for D=256 q4_0 V layers and packs/seals full K16 blocks with a small F16 tail buffer. FA-only and fail-closed for non-FA access. Not production evidence until native FA consumption, strict quality gates, and VRAM accounting pass. |
| `GGML_CUDA_ROCM_V4_K16D16_144_V_CACHE` | `1` | standard candidate | KV cache / CUDA pack op | Allocates internal `GGML_TYPE_V4_K16D16_144` storage for D=256 q4_0 V layers. Enabled by the standard V144/PV4 path unless explicitly set to `0` or hard-disabled with `GGML_CUDA_ROCM_V4_K16D16_144_PV4_DISABLE=1`. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PROFILE` | `0` | explicit DP16-DOT profile | DP16/QPack profile | Extra DP16-DOT/profile switch. Unset means off and must not disable or enable the standard scalar V144/PV4 path. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV_IMPL` | `scalar` | scalar selector | PDMQ | Selects the V144 P*V arithmetic implementation for the standard scalar V144/PV4 path. Mainline accepts only `scalar`. Experimental candidate names are kept out of this runtime option contract and live under the harness future-patches area. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4` | `1` | standard scalar V144/PV4 | KV cache / FA selector / PDMQ | Standard scalar V144/PV4 path when compiled. Set `0` for runtime controls. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4_DISABLE` | `0` | rollback/control | PDMQ | Hard runtime opt-out for V144/PV4 PDMQ selection even when legacy `PV4=1` or profile envs are present. Use for A/B controls and stale shell-profile cleanup. |

## Rules

1. **No silent knobs.** New QBlock/PDMQ/WMMA env variables require a registry entry before use.
2. **Default-safe.** Candidate and diagnostic knobs default off. Promoted standard knobs may default on only after strict correctness, profile, and speed gates; they must retain an explicit opt-out when practical.
3. **One canonical name per feature.** Prefer `GGML_CUDA_ROCM_MTP_QBLOCK_*` for backend QBlock route knobs. `LLAMA_MTP_*` aliases are allowed only for existing user-facing controls.
4. **Candidate lifecycle.** Candidate knobs must be named `*_CANDIDATE`, must stay default-off, must report correctness and speed gates before demotion or promotion.
5. **Diagnostics are not routes.** `*_TRACE`, `*_PARITY`, `*_DEBUG_*`, and profile knobs must not write accepted state or change consumed outputs unless explicitly listed as candidate/route.
6. **No prefix amnesty.** Every live QBlock/PDMQ/WMMA env knob must have an exact registry row. Broad legacy families are not considered registered.

## Canonical run profiles

Artifact bundle reconstruction lives in `.harness/research/qblock-bundle-archaeology-20260617.md`. Treat the rows below as bundles, not as independent knobs.

| Bundle | Artifact evidence | Decision |
| --- | --- | --- |
| Current no-env recurrent-prefix QBlock standard | Strict no-route-env n32/n128/n512 pass: `qprog-qblock-standard-strict-n32-20260617-093302`, `...n128-20260617-093344`, `...n512-20260617-093513`; no-diagnostic n512 `qprog-qblock-standard-no-diagnostics-n512-20260617-094126` = `11.540293317291123 tok/s`, `sha=df54c11d`, diagnostics off. | Standard runtime bundle. |
| Historical explicit GQA1/8x32/non-split serial-QKV | `qprog-qblock-serial-qkv-batched-fa-speed-ab-n512-20260616-160453/serial_qkv_batched_fa` = `11.515631401282532 tok/s`; PVBlockExact default-on artifact `qprog-qblock-pvblockexact-defaulton-vs-optout-speed-ab-n512-20260617-090423/02-default-pvblockexact` = `11.82711948333598 tok/s`; current explicit replay `qprog-qblock-explicit-canonical-gqa1-nondiag-n512-20260617-104717/run` = `11.374450119204015 tok/s`. | Equivalent/rollback recipe, not a hidden faster preset. |
| GQA6/split64 roweq-fastcap5 | Strict compare pass in `qprog-nmax6-roweq-fastcap5-working-20260616-014804/01-compare-n128`; valid ctx12288 speed `03-speed-n512-ctx12288` = `9.739972968911736 tok/s`; exact-only vs fastcap5 A/B neutral at ~`9.76 tok/s`. | Strict-passing experiment but slower than standard; do not promote. |
| prompt-`x` row-commit/spec-default smoke | Historical `step43-standard-prompt-x-qblock-default-n512-20260615-211055` = `55.27962529051494 tok/s`; current repro `qprog-promptx-step43-repro-current-n512-20260617-095822/run` = `62.28828673634196 tok/s`. | Different prompt/mode; not strict recurrent-prefix standard. |
| Goal55 pure-MTP active-RS candidate | `goal55-puremtp-rs-gate-20260622-163506`: n128 `53.91264719100367 tok/s`, n512 `43.29012381482717 tok/s`, hashes `accabc9a`/`8d10ba2d`, target/draft `n_rs_seq=[4,0]`. Standard PV4 profile `goal55-standard-pv4-profile-final-20260622-185554`: n128 `53.501416951589576 tok/s`, n512 `42.720123781558655 tok/s`, same hashes, target/draft `n_rs_seq=[4,0]`. `goal55-puremtp-rs-n512-cycle-20260622-164515`: n512 draft acceptance `357/612`, 255 rejected drafts, worst 16-output windows at 0.25-0.42 acceptance. | Candidate speed profile. Must not use `--spec-default`; `--spec-default` mixes ngram with MTP, disables target `n_rs_seq`, and drops n512 to ~36 tok/s. Remaining gap is proposal cadence/waste, not MMQ cap. |
| Direct graph side-effect/batch/group-row bundles | Strict n32 can pass, but n128 no-diagnostic speed loses to standard: side-effect `15.18000821380757`, batch-row `15.14943676642847`, group-row `15.243800774956723` tok/s vs standard n128 `15.824584481027806`. | Frozen legacy/debug; not promotion path. |
| Unsafe/stale shortcuts | accepted-row-only commit fails pre-repair state match; serial-QKV opt-out fails strict state despite faster no-compare n128; full-attn batch opt-out is slower. | Rejected or rollback-only. |

### Standard QBlock route, no diagnostics

Default standard route for Qwen35/Qwen3.6 MTP verifier blocks: QBlock recurrent-prefix backend, QBlock prefix full-attn batching, serial-QKV batched FA on validated non-terminal full-attn layers, packed16 DOT4/MMQ PDMQ, and PVBlockExact where supported. Do not enable compare/trace/profile knobs for a normal run.

Rollback/forcing knobs:

```bash
LLAMA_MTP_QBLOCK_RECURRENT_PREFIX=0                  # fall back from QBlock recurrent-prefix backend
LLAMA_MTP_QBLOCK_PREFIX_FULL_ATTN_BATCH=0            # fall back from QBlock prefix full-attn batching
LLAMA_MTP_QBLOCK_PREFIX_FULL_ATTN_SERIAL_QKV_BATCHED_FA=0  # fall back from serial-QKV batched FA
GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_PVBLOCK_EXACT=0    # fall back from PVBlockExact
```

### Strict compare sanity

Use this to prove strict scalar/QBlock parity after source changes. These are diagnostic knobs, not standard runtime defaults.

```bash
LLAMA_MTP_VERIFY_COMPARE=1
LLAMA_MTP_QBLOCK_TRACE=1
LLAMA_MTP_QBLOCK_RECURRENT_PREFIX=1
LLAMA_MTP_DISABLE_PACKED16_FA=0
LLAMA_MTP_ENABLE_FA=1
GGML_CUDA_ROCM_MTP_QBLOCK_GQA_GROUP=1
GGML_CUDA_ROCM_MTP_QBLOCK_SPLITK=1
GGML_CUDA_ROCM_MTP_QBLOCK_SPLITK_MIN_NK=0
# Optional shape override only when the test name says so:
# GGML_CUDA_ROCM_MTP_QBLOCK_SHAPE=8x32
```

Required result fields: HTTP 200, `rc=0`, token hash unchanged, `candidate_state_ok=1`, `oracle_state_ok=1`, pre/post state match, `state_match_all=true`.

### Whole-target QBlock verifier candidate

Removed from the live source tree during the 2026-06-23 desloppify pass. The orphan public `llama_decode_qblock_verify` API and uncompiled `src/llama-qblock-direct.inc` implementation were archived out-of-tree under the harness `archived-code/20260623-qblock-direct-orphan-api/` artifact. Do not document or use `LLAMA_MTP_QBLOCK_WHOLE_TARGET_*` envs unless a compiled verifier API is intentionally restored.

### State compare telemetry

Diagnostic-only state span comparison. These aliases reuse the existing Direct layer-compare implementation without enabling Direct graph execution.

| Env | Default | Status | Scope | Notes |
| --- | --- | --- | --- | --- |
| `LLAMA_MTP_QBLOCK_STATE_LAYER_COMPARE` | `0` | diagnostic | server trace | Neutral alias for state span compare telemetry. Emits `QBLOCK_STATE_LAYER_COMPARE` lines. |
| `LLAMA_MTP_QBLOCK_STATE_LAYER_COMPARE_LIMIT` | `-1` | diagnostic | server trace | Optional max layer index for neutral state compare. Falls back to old Direct limit when unset. |
| `LLAMA_MTP_QBLOCK_EXACT_COMMIT_LAYER_COMPARE` | `0` | diagnostic | server trace | Exact-commit bring-up alias for the same compare helper. Emits `QBLOCK_EXACT_COMMIT_LAYER_COMPARE` lines. |
| `LLAMA_MTP_QBLOCK_EXACT_COMMIT_LAYER_COMPARE_LIMIT` | `-1` | diagnostic | server trace | Optional max layer index for exact-commit compare. |
| `LLAMA_MTP_QBLOCK_RS_STAGE_TRACE` | `0` | diagnostic | recurrent trace | Neutral alias for existing R/S staging trace telemetry. Emits `QBLOCK_RS_STAGE_TRACE` lines. |
| `LLAMA_MTP_QBLOCK_RS_STAGE_TRACE_LAYER` | `-1` | diagnostic | recurrent trace | Optional layer filter for neutral R/S staging trace. |
| `LLAMA_MTP_QBLOCK_RS_STAGE_TRACE_KIND` | unset | diagnostic | recurrent trace | Optional component filter, usually `R` or `S`. |
| `LLAMA_MTP_QBLOCK_EXACT_COMMIT_RS_STAGE_TRACE` | `0` | diagnostic | recurrent trace | Exact-commit bring-up alias for R/S staging trace. Emits `QBLOCK_EXACT_COMMIT_RS_STAGE_TRACE` lines. |
| `LLAMA_MTP_QBLOCK_EXACT_COMMIT_RS_STAGE_TRACE_LAYER` | `-1` | diagnostic | recurrent trace | Optional layer filter for exact-commit R/S staging trace. |
| `LLAMA_MTP_QBLOCK_EXACT_COMMIT_RS_STAGE_TRACE_KIND` | unset | diagnostic | recurrent trace | Optional component filter for exact-commit R/S staging trace. |

Legacy compatibility remains available through `LLAMA_MTP_QBLOCK_DIRECT_LAYER_COMPARE`, `LLAMA_MTP_QBLOCK_DIRECT_LAYER_LIMIT`, and `LLAMA_MTP_QBLOCK_DIRECT_RS_STAGE_TRACE*`, but new strict-performance work should prefer the neutral names above.

Forced UBATCH1 verifier decode now regenerates recurrent rollback snapshot rows by copying the live R/S row to the matching rollback slot after each one-token verifier ubatch. This is not a new route knob; it is required so `LLAMA_MTP_TARGET_BATCH_VERIFY_UBATCH1=1` diagnostics can make partial/zero accepts strict-state exact instead of copying stale snapshot rows. Existing `LLAMA_MTP_RS_COMMIT_TRACE` / `LLAMA_MTP_RS_COMMIT_LAYER_TRACE` diagnostics also emit `LLAMA_RS_SNAPSHOT_TRACE` / `LLAMA_RS_SNAPSHOT_LAYER_TRACE` lines when this path snapshots rows.

### Draft branch-candidate metadata

Default-off server-visible metadata channel for future tree/block verifiers. It records candidate token/logit/rank metadata next to `spec_draft` without changing sampler token selection. Normal draft paths capture real draft top-k rows; target-sidecar direct paths publish only rank0 selected-token metadata so they cannot masquerade as alternate branches.

Transport path: server maps the selected verifier rows to optional `llama_batch` / `llama_ubatch` QBlock row metadata arrays (`parent`, `branch_id`, `candidate_rank`, `output_policy`), `llama-graph.cpp` packs those rows into `FLASH_ATTN_EXT` op params, and the PDMQ/QBlock backend applies them to `dp16_fa_qblock_program` before route trace/kernel launch. Route logs expose `qprog_meta_rows`. The PDMQ/QBlock backend now treats local verifier-window K rows as tree-visible only when they are the current row or an ancestor under `row_parent`; past context remains visible. At the FA level, `ATTENTION_ONLY` still writes attention output, while server `logits=false` suppresses final logits for sibling sidecars.

| Env | Default | Status | Scope | Notes |
| --- | --- | --- | --- | --- |
| `LLAMA_MTP_DRAFT_BRANCH_CANDIDATES` | `0` | metadata | speculative draft/server | Enables per-draft-depth candidate capture in `common_speculative_draft_params::branch_candidates`. When enabled without an explicit `LLAMA_MTP_BACKEND_TOPK_K`, backend top-k width defaults to 16 for metadata only; sampled token selection remains unchanged. |
| `LLAMA_MTP_DRAFT_CANDIDATES_TRACE` | `0` | diagnostic | server trace | Enables branch-candidate capture and emits `MTP_DRAFT_CANDIDATES` lines from `server_slot::update_batch()`. Diagnostic only; do not use for clean speed gates. |
| `LLAMA_MTP_QBLOCK_SIBLING_ROWS_PROTOTYPE` | `0` | experimental | server/QBlock verifier | Single-slot-only prototype that appends non-selected candidate rows after the selected verifier path. Rows are not added to `spec_i_batch`, use `logits=false`, and carry `ATTENTION_ONLY` output policy. Token selection is unchanged. |
| `LLAMA_MTP_QBLOCK_SIBLING_ROWS_MAX` | `1` | experimental | server/QBlock verifier | Maximum sibling sidecar rows to append when the prototype is enabled; clamped to 8. |
| `LLAMA_MTP_QBLOCK_SIBLING_ROWS_TRACE` | `0` | diagnostic | server/QBlock verifier | Emits `MTP_QBLOCK_SIBLING_ROWS` summaries. Candidate trace also enables this summary. |
| `LLAMA_MTP_QBLOCK_SIBLING_LOGITS_PROBE` | `0` | diagnostic/probe | server/QBlock verifier | Safe parent-logit hit detector for sibling candidates. It enables branch-candidate capture, scopes raw verifier logits via `LLAMA_MTP_TOPK_VERIFY=1`, disables target fused-top1 sampled buffers for the verifier decode, and emits `MTP_QBLOCK_SIBLING_TOP1 source=parent_logits` with sibling candidate top1/rank. It deliberately suppresses same-batch sibling sidecar rows because those rows can perturb verifier trajectory and their logits predict the token after the sidecar, not the sidecar candidate itself. |
| `LLAMA_MTP_QBLOCK_SIBLING_BRANCH_PLAN_TRACE` | `0` | diagnostic/probe | server sampler | Emits `MTP_QBLOCK_BRANCH_PLAN` after target sampling. It reports whether the actual sampled token at the first rejected draft depth was present in captured sibling candidates. `safe_commit=0` is intentional until same-cycle branch descendants and transactional tree commit exist. The logits probe enables this trace automatically. |
| `LLAMA_MTP_QBLOCK_SIBLING_BRANCH_STATE_TRACE` | `0` | diagnostic/probe | server sampler/spec state | Emits `MTP_QBLOCK_BRANCH_STATE` for sibling-hit rejects. It distinguishes next-cycle readiness from same-cycle branch extension: the emitted sampled token and parent-row hidden seed are available for the normal next cycle, but same-cycle extension remains unavailable because draft branch descendants, target branch rows, and transactional tree commit are not captured yet. The logits probe enables this trace automatically. |
| `LLAMA_MTP_QBLOCK_SIBLING_BRANCH_DESC_TRACE` | `0` | diagnostic/probe | server sampler/spec state | Emits `MTP_QBLOCK_BRANCH_DESC` for sibling-hit rejects. It records selected-path rows already available after the rejected parent and explicitly reports that sampled-sibling branch descendants and target branch rows are not captured (`same_cycle_replayable=0`, `safe_commit=0`). Branch-state tracing enables this trace automatically. |
| `LLAMA_MTP_QBLOCK_SIBLING_BRANCH_SUBTREE_TRACE` | `0` | diagnostic/probe | server sampler/spec state | Emits `MTP_QBLOCK_BRANCH_SUBTREE` for sibling-hit rejects. It records the sampled sibling root candidate, selected-path row/token lists, and bounded candidate lists, but remains trace-only: sampled-sibling descendants and target branch rows are still uncaptured in this root marker, with `same_cycle_replayable=0` and `safe_commit=0`. Branch-desc tracing enables this trace automatically. |
| `LLAMA_MTP_QBLOCK_SIBLING_DESC_PROBE` | `0` | diagnostic/probe | MTP draft/server sampler | Emits `MTP_QBLOCK_BRANCH_DESC_PROBE` for sibling-hit rejects. It snapshots/restores the draft sequence state, rewinds the draft KV to the sampled-sibling position, temporarily seeds MTP `pending_h` from the target parent row, and drafts sampled-sibling descendants without touching target KV, production sampler, prompt, output, or commit state. Still proof-only: target branch rows are missing, so `same_cycle_replayable=0` and `safe_commit=0`. |
| `LLAMA_MTP_QBLOCK_SIBLING_DESC_MAX` | `1` | diagnostic/probe | MTP draft/server sampler | Max sampled-sibling descendants for `LLAMA_MTP_QBLOCK_SIBLING_DESC_PROBE`; clamped to `1..8`. The 9B proof runner uses `1` to prove one transaction-tail replacement draft token per sibling-hit opportunity. |
| `LLAMA_MTP_QBLOCK_SIBLING_TARGET_ROWS_PROBE` | `0` | diagnostic/probe | target verifier/server sampler | Emits `MTP_QBLOCK_BRANCH_TARGET_PROBE` for sibling-hit rejects after descendant capture. After normal sampling, it clears a reserved scratch sequence, copies the immutable pre-verifier attention prefix into it, remaps the saved pre-verifier recurrent checkpoint onto the scratch seq, then serially replays `sampled + accepted selected-prefix + sampled sibling + sampled-sibling descendants` there to capture target branch rows/logits. It does not mutate the production sequence, call `common_speculative_process()`, or touch draft state, production sampler, prompt, output, or commit state. Same-cycle commit stays disabled. |
| `LLAMA_MTP_QBLOCK_SIBLING_TARGET_ROWS_MAX` | `1` | diagnostic/probe | target verifier/server sampler | Max sampled-sibling descendants to include in `MTP_QBLOCK_BRANCH_TARGET_PROBE`; clamped to `1..8`. With the default descendant probe depth, the 9B proof captures two target branch rows per sibling hit: the sampled sibling row and one descendant row. |
| `LLAMA_MTP_QBLOCK_SIBLING_TARGET_ROWS_ORACLE_COMPARE` | `0` | diagnostic/probe | target verifier/server sampler | When target-row probing is active, reuses the reserved scratch seq for a second normal row-serial replay of the exact same branch token/position list, compares branch-row top1/top2/watch rank/logit and compact raw-logit digest against the first captured rows, and appends `oracle_*` fields to `MTP_QBLOCK_BRANCH_TARGET_PROBE`. Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_SIBLING_TARGET_ROWS_SAMPLER_ORACLE` | `0` | diagnostic/probe | target verifier/server sampler | When target-row probing is active, clones the pre-accept production sampler (`smpl_save`), accepts the selected-prefix tokens plus sampled sibling on the clone only, and samples scratch target branch-row logits (`idx=0`) with `common_sampler_sample`/`common_sampler_accept`. It appends `sampler_oracle_*` fields and token lists to `MTP_QBLOCK_BRANCH_TARGET_PROBE`; mismatches are evidence about branch-tail viability, not production mutations. The same probe also replays the sampler-oracle token list on the scratch target seq and appends `sampler_replay_*` row/logit/state fields, proving the branch sampler tail can be materialized independently of the draft descendant tail. Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_BRANCH_TXN_SAMPLER_COMMIT` | `0` | guarded prototype gate | server sampler/spec state | Enables the first server-side sampler-only transaction mutation for sibling-hit QBlock rejects. It requests draft branch-candidate capture and branch-plan staging so the env can observe sibling hits by itself. When the final committed output bundle contains the sampled sibling at the expected rejected-depth index, the server clones the pre-accept sampler (`smpl_save`), accepts the exact committed output token list on the clone with `common_sampler_accept`, then swaps `slot.smpl` to that clone immediately before prompt/output mutation. It emits `MTP_QBLOCK_BRANCH_TXN_SAMPLER_COMMIT` with clone/final-invariant fields and `sampler_touched=1` only on the guarded swap. It does not mutate KV, recurrent state, prompt, draft, target context, or output, and keeps `same_cycle_replayable=0`/`safe_commit=0` until KV + recurrent transaction wiring exists. Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_BRANCH_TXN_RECURRENT_COMMIT` | `0` | guarded prototype gate | server recurrent state | Enables the first recurrent branch-state transaction proof. It implies branch-candidate capture, branch-plan staging, descendant probing, target-row scratch replay, and sampler-oracle/prefix-only replay so a scratch sequence can materialize branch recurrent state. The guarded import uses the scratch sampler-replay **prefix** state, one token before the final emitted token, and only calls `llama_state_seq_set_data_ext(..., LLAMA_STATE_SEQ_FLAGS_PARTIAL_ONLY | LLAMA_STATE_SEQ_FLAGS_ALLOW_SEQ_REMAP)` when the scratch replay output token list exactly matches the final committed output bundle and the prefix-token count matches. No-tail sibling bundles can replay just `prefix + sampled sibling`; branch tails still require sampler-oracle rows. Because KV branch commit is not wired yet, the marker runs after the normal recurrent rollback commit, backs up the post-commit recurrent state, imports the branch prefix state, verifies canonical post-import digest, then restores the backup. `recurrent_touched=1 recurrent_restored=1 production_mutation=0` means the import API was exercised but final production state was rolled back to preserve hash parity. It does not mutate KV, sampler, prompt, draft, or output, and keeps `same_cycle_replayable=0`/`safe_commit=0` until KV transaction wiring exists. Requires a scratch sequence to be available (for example `--kv-unified` with enough target `n_seq_max`). Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_BRANCH_TXN_KV_SPLIT_PROOF` | `0` | diagnostic/probe | server target memory | Captures the sampled-branch **full** sequence state on the scratch replay prefix, imports it onto the production sequence after normal commit, restores the production post-commit recurrent partial state, verifies that full state changed while recurrent state matches pre-import, then restores the original full state. Emits `MTP_QBLOCK_BRANCH_TXN_KV_SPLIT status=ok production_mutation=0`. This is proof-only and expensive because it serializes full sequence state; not a speed gate. |
| `LLAMA_MTP_QBLOCK_BRANCH_TXN_KV_SPLIT_COMMIT` | `0` | guarded candidate gate | server target memory | Same split operation as proof mode, but leaves the branch full-state import active after restoring production recurrent partial state. This commits branch attention/KV while keeping the normal production recurrent state, emits `MTP_QBLOCK_BRANCH_TXN_KV_SPLIT status=candidate_committed production_mutation=1`, and still reports `safe_commit=0` until broader default-off gates and route cleanup promote it. Default off; validated locally on n128/n512 canonical hashes. |
| `LLAMA_MTP_QBLOCK_BRANCH_TXN_KV_PHYSICAL_IMPORT_DIRECT_REPLAY` | `1` | guarded candidate subgate | server target memory | Applies only when `LLAMA_MTP_QBLOCK_BRANCH_TXN_KV_PHYSICAL_IMPORT_COMMIT=1` is the sole transaction path and no explicit target-row diagnostics are requested. It skips the diagnostic target-row probe decode and goes directly to the scratch replay needed by physical KV import. Set `0` to restore the older probe-before-replay path for comparison. |
| `LLAMA_MTP_QBLOCK_SIBLING_TXN_PROOF` | `0` | diagnostic/probe | server sampler/spec state | Emits `MTP_QBLOCK_BRANCH_TXN_PROOF` immediately after the final accepted bundle is materialized and before prompt rewrite, `slot.sampled` update, output emission, sampler replacement, or context cleanup. It compares the ordinary output tail after the sampled sibling with the staged sampled-sibling descendant tokens, checks that scratch target rows/logits, sampler-oracle comparisons, and sampler-oracle branch replay cover that tail, and prints pre-rewrite prompt-tail lists. It also emits `MTP_QBLOCK_BRANCH_TXN_REPLAY_COMPARE`, a metadata-only compare of the ordinary final bundle against the sampler-oracle branch bundle plus the already-captured branch replay state digest; extra post-commit scratch decodes are intentionally avoided because they can perturb recurrent commit state. Branch-token/state gaps are reported as blocker evidence while structural list/captured-branch-state failures remain fail-closed. Both markers hard-code `production_mutation=0`, `same_cycle_replayable=0`, and `safe_commit=0`. Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_REPLAY_ORACLE_TRACE` | `0` | diagnostic/probe | server sampler/spec state | Emits lightweight `MTP_QBLOCK_REPLAY_ORACLE` lines for each accepted target-batch verify bundle before replay/row-only commit handling mutates prompt/output state. It records slot, verifier backend, draft/accepted/rollback counts, checkpoint token and position bounds, canonical commit position range, sampled token, replay/row-only flags, and the accepted token bundle. When serial replay actually runs, it also emits `MTP_QBLOCK_REPLAY_ORACLE_DONE` with replay position span, target/draft pos-max values, replay flags, and recurrent state digest if available. Use these markers as oracle/trail sources for correlating replay/canonical commit ranges with tail-page consumer `ok`/`no_registered_map` coverage. It does not force replay, allocate scratch sequences, or change accept/commit semantics. Default off. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PROOF` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_TXN_TAIL_PAGE` from the packed16 DOT4/PDMQ QBlock route. It performs static fail-closed eligibility checks for the transactional packed16-K + V4_144 tail-page ABI, builds a one-page aligned tail descriptor, validates the current QBlock verifier positions as an implicit linear lineage, runs the single-owner page-state/free-list metadata commit model on a transaction-local page id, reports descriptor/lineage/state status plus `state_valid_tail`, `state_page_valid`, and `state_free`, and also emits `MTP_QBLOCK_TXN_TAIL_WRITE` from packed16-K/V4_144 cache writes. The write marker reports the exact 16-token page base/end, payload slot range, whether the write spans pages, how many page slots are not supplied by the current write (`slots_before`/`slots_after`), and the per-kind row/page/payload/merge-copy byte counts so partial-page staging requirements are explicit. It also emits proof-only `MTP_QBLOCK_TXN_TAIL_STAGE` lines that validate a separate producer-side staging descriptor for one-page read/merge/write metadata, plus `MTP_QBLOCK_TXN_TAIL_BACKEND` lines from the actual packed16-K and V4_144 backend pack ops to record source/destination stride/layout geometry and, for tiny verifier writes, copy at most 8 device-side row indices to prove the backend page/slot range when the stream is not graph-capturing (`idx_capture_skipped=1` during CUDA graph capture). When indices are copied, backend pack ops also emit `MTP_QBLOCK_TXN_TAIL_ADDR` with producer-side payload/scale byte spans for packed16-K and V4_144 page slots. Proof mode is accounting-only: it does not allocate scratch, copy KV data, or alter dispatch. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGE_AUTHORITY` | `0` | guarded candidate | host KV cache + packed16/V4_144 FA backend | Default-off cache-owned full-current-K page authority for DBV paged attention. Under `GGML_CUDA_ROCM_PACKED16_DBV_PAGED_ATTENTION=1`, `llama_kv_cache` owns a persistent 16-token block table, generation, and CUDA device mirror; packed16 K/V producer index inputs can be remapped by `logical_token -> physical_page*16 + slot`; DBV consumers read the authority mirror through the existing full-map ABI. Identity tables remain explicit tables on this path, not an identity fallback. Missing authority still fails closed through strict full-map checks. No promotion until hash/draft routeproof and speed gates pass. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGE_AUTHORITY_TRACE` | `0` | diagnostic/probe | host KV cache + packed16 registry | Emits `MTP_QBLOCK_PAGE_AUTHORITY` and `MTP_QBLOCK_FULL_PAGE_AUTHORITY` lines for authority invalidate/rebuild/publish/commit/rollback, device-table upload/reuse, and consumer get. It also enables the packed16 registry trace lines needed to distinguish authority misses from old tail-page registry misses. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION` | `0` | guarded production candidate | packed16/V4_144 FA backend | Single production-line opt-in for read-side tail-page attention. It enables the packed16/V4_144 tail-page writer, global producer-map publication, FA consumer page-table reads, and non-scratch map preservation across provably disjoint `seq_rm()` cleanup without enabling server `MTP_QBLOCK_TXN_TAIL_PAGE_RUNTIME` branch-transaction metadata, sampler/recurrent/KV commit proof, state hash/restore, or replay machinery. The hot path remains producer-owned page-table publication plus FA `tail_page_mode=TABLE` K/V address translation for non-identity maps; visible identity tail maps now bypass table mode as `identity_tail_no_table` because canonical write-through already makes physical==logical bytes authoritative. Unsupported layouts, overlapping cleanup, and missing maps still fail closed to canonical addressing. Validated: identity-bypass speed rerun n128 `/qblock-paged-prod-speed-ab-n128-20260627-044741` baseline 48.40 vs paged 48.23 tok/s, ratio 0.9964, trace 154 `identity_tail_no_table`, runtime0/branch0, hash `accabc9a`; n512 `/qblock-paged-prod-speed-ab-n512-20260627-044921` baseline 38.68 vs paged 38.64 tok/s, ratio 0.9992, runtime0/branch0, hash `8d10ba2d`. Earlier pre-bypass proof: n128 trace `qblock-paged-attention-prod-n128-20260626-010826` hash `accabc9a`, active/bound/ok 154, strict binds 154, runtime0/branch0, 47.37 tok/s; n512 trace `qblock-paged-attention-tests-20260626-012857/n512_paged_trace` hash `8d10ba2d`, active/bound/ok 392, strict binds 392, runtime0/branch0, 38.80 tok/s; clean n128 A/B baseline 48.03 vs paged 48.00 tok/s, ratio 0.9993. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE` | `0` | guarded candidate subgate | packed16/V4_144 FA backend | First bounded write-side paging slice under `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION=1`: eligible partial tail K/V writes still update canonical rows, then also write the same rows into a reserved non-visible physical page and publish a non-scratch `OWNED_TAIL_WRITE` map to that page. This proves FA can consume bytes from a non-canonical physical page while canonical rows remain a fallback; it is intentionally not the exclusive/no-write-through promotion yet. The FA consumer rejects the map as `owned_page_visible` if the chosen physical page is inside the current visible `nk` range. The producer graph-build path now preclaims the same physical page before `ggml_pack_k_packed16`/`ggml_pack_v4_k16d16_144` run by initializing an absolute empty paged state, claiming the reserved physical page as TXN, committing it to CANONICAL, and publishing that owned map; the CUDA K/V writers consume the reserved map when present and otherwise fall back to the old last-page formula unless `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_REQUIRE_PREPUBLISHED_MAP=1` is set. Registry trace emits `op=owned_preclaim_commit`. The branch-runtime producer-state-import path also treats owned maps as cache-owned metadata via absolute empty state → TXN claim → CANONICAL commit, with rollback/clear on failure; registry trace emits `op=owned_claim_commit`. Validated preclaim gates: n128 trace `qblock-paged-attention-owned-preclaim-n128-20260626-025417` hash `accabc9a`, `owned_preclaim_ok=89`, strict owned dispatch 154, runtime0/branch0; n128 clean `qblock-paged-attention-owned-preclaim-clean-n128-20260626-025502` hash `accabc9a`, 49.59 tok/s; n512 trace `qblock-paged-attention-owned-preclaim-n512-20260626-025600` hash `8d10ba2d`, `owned_preclaim_ok=196`, strict owned dispatch 392, runtime0/branch0. Readiness proof now records K and V writer acceptance for the same owned map before any exclusive skip is allowed: n128 `qblock-paged-attention-owned-ready-n128-20260626-031359` hash `accabc9a`, K/V ready 89/89, pair-ready 174, strict dispatch 154; n512 `qblock-paged-attention-owned-ready-n512-20260626-031508` hash `8d10ba2d`, K/V ready 196/196, pair-ready 388, strict dispatch 392; clean n128 after instrumentation `qblock-paged-attention-owned-preclaim-clean-n128-20260626-031602` hash `accabc9a`, 48.27 tok/s. The diagnostic exclusive env below was added but currently stays fail-closed for this base0 single-map route: n128 `qblock-paged-attention-owned-exclusive-n128-20260626-033234` hash `accabc9a`, exclusive K/V ok 0/0, skips were `kv_pair_not_ready` or `no_route_highwater`; n512 `qblock-paged-attention-owned-exclusive-failclosed-n512-20260626-033530` hash `8d10ba2d`, exclusive K/V ok 0/0; clean n128 with exclusive env `qblock-paged-attention-owned-exclusive-clean-n128-20260626-033651` hash `accabc9a`, 47.33 tok/s. Validated n128 import proof `qblock-tail-page-owned-claim-stateimport-n128-20260626-023352` hash `accabc9a`, `owned_claim_commit=6`, `map_flags=0x2`, `bytes_authoritative_reason=ok`, runtime map registered 6/6, dispatch_active 0. Default off until a later route-complete page table removes canonical writes safely. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_DUAL_DEST_CANDIDATE` | `0` | experimental candidate | packed16/V4_144 K/V writers | Subgate under paged attention + owned-tail write. When set, eligible non-exclusive owned-tail K and V writers compute/pack once and write the same packed result to both canonical rows and the pre-published non-visible owned overlay page. This preserves canonical write-through/commit-back authority and does not authorize no-write-through, route-complete, or exclusive skipping. It is default-off because end-to-end speed evidence is mixed/noisy: `/qblock-owned-dual-dest-candidate-ab-20260627-014344` preserved hashes n128 `accabc9a`, n512 `8d10ba2d`, ratio n128 `0.998`, n512 `1.021`; reversed `/qblock-owned-dual-dest-candidate-rev-ab-20260627-014621` preserved hashes with ratio n128 `1.000`, n512 `0.999`. Correctness-clean, not promoted. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_REQUIRE_PREPUBLISHED_MAP` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Subgate under paged attention + owned-tail write. When set, an eligible K/V owned-tail writer with no pre-published non-scratch owned map covering the exact logical tail fails closed instead of using the old last-page/scratch fallback as an owned destination. It emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_FAIL_CLOSED kind=K/V reason=missing_prepublished_owned_map`, leaves canonical write-through active through the existing merge path, and does not feed exclusive/no-write-through authorization. Default off so historical proof scripts can still classify `legacy_fallback_owned`; use it for map-authority validation runs once preclaim coverage is expected. GPU require-map proof passed with zero fail-closed gaps: n128 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n128-20260626-201953` hash `accabc9a`, `writer_from_map` 178/178, `legacy_fallback_owned` 0, `fail_closed_missing_map` 0, consumer no-map0; n512 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n512-20260626-202024` hash `8d10ba2d`, `writer_from_map` 406/406, fallback0, fail-closed0, no-map0. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_CANONICAL_POISON` | `0` | destructive diagnostic/probe | packed16/V4_144 FA backend | Subgate under paged attention + owned-tail write. After a K/V owned-tail writer has written the same logical rows into the non-visible owned physical page, this launches an extra diagnostic kernel that intentionally corrupts the original canonical K/V rows addressed by the source indices, then emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_CANONICAL_POISON kind=K/V`. It does not change map publication, FA bind decisions, page-table address translation, readiness, route-complete, or exclusive authorization; the only purpose is to prove whether any live path still consumes canonical rows. Use with `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_REQUIRE_PREPUBLISHED_MAP=1` and `GGML_CUDA_ROCM_MTP_QBLOCK_TAIL_PAGE_LOGICAL_SLOT_AUDIT=1`. If hashes stay clean, canonical write-through is likely redundant for the covered tail; if hashes change, canonical rows are still consumed somewhere and no-write-through promotion is unsafe. K-only/V-only aliases are also accepted: `...CANONICAL_POISON_K`, `...CANONICAL_POISON_V`, plus legacy spelling `...POISON_CANONICAL[_K|_V]`. Default off and never production-safe. Canonical-dependence proof failed promotion: K+V poison n128 `/qblock-canonical-poison-kv-20260626-210255/n128_kv` changed hash `accabc9a→82daf46c`; K-only/V-only localization also changed hashes at n128 (`0bf1cc3a`, `60e35d13`) and n512 (`df00ee05`, `3996753a`) while audit bad/visible-noncanonical stayed 0 and `legacy_fallback_owned=0`, proving canonical K and V rows are still live somewhere. Keep canonical write-through enabled; do not promote no-write-through. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_CONSUMER_READ_MODE_TRACE` | `0` | proof-only diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_CONSUMER_READ_MODE` from packed16 DOT4-MMQ, q8k DOT4 fallback, and packed16 WMMA launch families. It reports `read_mode=tail_page|canonical`, backend, node/layer/graph instance, shape/variant, Q/K sizes, and tail-page bind/map fields. Localization proof for n128 `[7730,7770)` K poison showed only DOT4-MMQ launches and all were `read_mode=tail_page`; no q8k/WMMA markers appeared, and disabling GQA6 wavegroup stayed hash-dirty. Follow-up persistence tracing emitted zero state/import lines; dynamic parsing showed the actual blocker is aged-out rows outside the bounded tail map, not an unbound FA launch or host state persistence. Alias `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_CONSUMER_READ_MODE_TRACE` is accepted. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TAIL_PAGE_AGE_OUT_TRACE` | `0` | proof-only diagnostic/probe | packed16 DOT4-MMQ FA backend | Emits `MTP_QBLOCK_TAIL_PAGE_AGE_OUT` from the packed16 DOT4-MMQ launch setup. It classifies a watched logical row interval against the current bounded tail-page map as `aged_out`, `covered`, `after_tail`, `not_visible`, `no_map`, or `no_watch`, and reports visible/covered/aged-out token counts plus map base/tail/generation fields. The watch interval reuses `GGML_CUDA_ROCM_MTP_QBLOCK_KV_PERSISTENCE_TRACE_MIN_IDX` / `_MAX_IDX`, falling back to `...OWNED_TAIL_WRITE_CANONICAL_POISON_MIN_IDX` / `_MAX_IDX`; this makes the next canonical-poison GPU run count the age-out condition directly instead of requiring external log parsing. Alias `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_AGE_OUT_TRACE` is accepted. This marker is launch-side logging only and does not change page maps, K/V writes, address translation, or canonical write-through. CPU contract `test_owned_ageout_requires_canonical_write_through_contract` pins the production invariant: rows outside the current 4-page consumer map require canonical write-through/commit-back; a 5-page host state can still resolve them internally but exports `MAP_TOO_LARGE` to the current FA consumer ABI. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_KV_PERSISTENCE_TRACE` | `0` | proof-only diagnostic/probe | host KV cache persistence/import | Emits `MTP_QBLOCK_KV_PERSISTENCE` from `llama_kv_cache::state_write_data`, `state_read_data`, and `seq_import_physical`. It labels canonical K/V serialization/restoration, packed16 sidecar non-serialization/non-restoration, physical-import branch choice (`rebind` vs fallback), and fallback PDMQ sidecar copy policy. Optional filters `GGML_CUDA_ROCM_MTP_QBLOCK_KV_PERSISTENCE_TRACE_MIN_IDX` / `_MAX_IDX` limit logging by canonical row; if unset they inherit the canonical-poison min/max envs. Alias `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_KV_PERSISTENCE_TRACE` is accepted. This marker is host-only and does not alter map, state, or tensor bytes. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_READY_TRACE` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_READY` from the CUDA K and V pack writers when `OWNED_TAIL_WRITE` is active. A K/V writer reports `status=ok` only after the backend page-merge predicate is true, the host-published owned map covers the same logical tail, the map is non-scratch owned, and the selected physical page is non-visible. The registry tracks K/V readiness against the exact same map/generation and emits `pair_ready=1` only when both writers have accepted that map; map clears, invalid registration, lifecycle reset, or a new map reset the readiness state fail-closed. This is proof-only and does not skip canonical writes. `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_REGISTRY_TRACE=1` also enables these lines. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_ROUTE_COMPLETE_TRACE` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_TXN_TAIL_PAGE_ROUTE_COMPLETE` sidecar accounting without changing the page-map ABI, exclusive gate, or canonical write-through policy. Host K-view registration publishes an expected route-layer count for the active map; the sidecar derives required logical pages from the existing 4-entry map and each successful FA dispatch bind records `(layer, logical_page)` coverage plus logical-token → physical-page/slot translation fields. `complete=1` now requires `bound_layer_pages >= expected_layers * expected_pages`, not just unique layer count. Map replacement, invalidation, clear, or lifecycle reset clears the sidecar fail-closed. This remains trace-only and does not authorize canonical-write skip. Historical pre-page-aware n128 trace `qblock-paged-attention-route-complete-n128-20260626-035915` preserved hash `accabc9a`; page-aware GPU trace `qblock-paged-attention-route-complete-n128-20260626-050054` preserved hash `accabc9a` for the earlier single-tail maps. Follow-up runtime route-complete now publishes rolling ≤4-page mixed canonical+owned block tables from both host preclaim and CUDA K producer, without map ABI expansion and without canonical-write skip; visible pages are accepted only when they are canonical self-maps, while visible noncanonical overlay pages still reject. The route-complete trace env also requests dispatch-bind recording, so expected and bind coverage are emitted together. GPU gates PASS: n128 `qblock-paged-attention-route-complete-n128-20260626-110323` hash `accabc9a`, expected-pages `{1:17,4:72}`, translated-pages `{1:17,4:137}`, complete binds for expected-pages=4 count 92, rejects0, dispatch/bind generation match; n512 `qblock-paged-attention-route-complete-n512-20260626-110412` hash `8d10ba2d`, expected-pages `{1:17,4:179}`, translated-pages `{1:17,4:375}`, complete binds for expected-pages=4 count 315, rejects0, dispatch/bind generation match. Host checks PASS: `git diff --check`, `test-pdmq-k-formats` build/run, and `llama-server` link. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_EXCLUSIVE` | `0` | guarded diagnostic/probe | packed16/V4_144 FA backend | Attempts to skip canonical K/V rows only for owned-tail writes after the same-map K+V readiness pair is already present, the route-complete sidecar is complete for that exact map, and the observed no-map consumer high-water proves the write is beyond uncovered historical-prefix FA scans. K and V emit `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_EXCLUSIVE`; the gate stays fail-closed when the pair is not ready, the map differs, same-map route-complete is absent (`reason=route_incomplete`), no route high-water has been observed, or V has not seen a matching K-exclusive decision. After rolling route-complete, explicit exclusive diagnostics still keep write-through because skip authorization is unavailable at K/V write time: n128 `qblock-paged-attention-owned-exclusive-routecomplete-n128-20260626-123939` hash `accabc9a`, route expected-pages `{1:17,4:72}`, K/V ready 89/89, exclusive K/V ok 0/0, blockers `kv_pair_not_ready`, `route_incomplete`, `no_route_highwater`; n512 `qblock-paged-attention-owned-exclusive-routecomplete-n512-20260626-124102` hash `8d10ba2d`, route expected-pages `{1:17,4:179}`, K/V ready 196/196, exclusive K/V ok 0/0 with the same blockers. Legacy high-water mode remains guarded by the observed no-map high-water. The separate all-bound subgate below can authorize when the owned-snapshot proof has eliminated no-map consumers, but still requires same-map K/V readiness and same-map route-complete. Do not default-enable until broader clean speed and longer-context gates pass. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_EXCLUSIVE_ALL_BOUND` | `0` | unsafe diagnostic first key | packed16/V4_144 FA backend | First key for the retired all-bound no-write-through probe under `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_EXCLUSIVE=1`. After the age-out proof, this env alone no longer authorizes canonical K/V row skipping; with no route high-water it fails closed as `reason=all_bound_unsafe_gate_disabled` unless the explicit unsafe archaeology key below is also set. Historical behavior allowed canonical K/V row skipping when there was no observed no-map high-water (`no_map_highwater=0`) because all FA consumers for the same owned map were bound, while still requiring a non-scratch owned map, physical-page match, range coverage, same-map K+V readiness, same-map route-complete, and K-before-V exclusive ordering. With `...OWNED_TAIL_WRITE_EXCLUSIVE_TRACE=1`, it emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_EXCLUSIVE ... reason=all_bound_no_miss all_bound_mode=1`; otherwise it does not force trace logging. First trace GPU gate is hash-clean with real skips: n128 `/qblock-paged-attention-all-bound-exclusive-n128-20260626-171501` hash `accabc9a`, consumer no-map 0, exclusive ok K/V 3/3, all-bound resolve status 140; n512 `/qblock-paged-attention-all-bound-exclusive-n512-20260626-171614` hash `8d10ba2d`, consumer no-map 0, exclusive ok K/V 4/4, all-bound resolve status 533. Clean/no-proof A/B after trace separation is hash-clean with zero tail-page proof/trace lines: `/qblock-paged-attention-all-bound-exclusive-clean-ab-20260626-173407` n128 baseline/candidate `accabc9a` 44.54/41.87 tok/s, n512 `8d10ba2d` 34.95/34.81 tok/s; n128 reversed repeat `/qblock-paged-attention-all-bound-exclusive-clean-n128-rev-20260626-173729` candidate/baseline 43.87/44.82 tok/s; n1024 coverage `/qblock-paged-attention-all-bound-exclusive-clean-n1024-ab-20260626-173917` baseline/candidate hash `b6f526c0`, speed 32.13/32.10 tok/s. Post-cleanup clean A/B `/qblock-paged-attention-all-bound-exclusive-clean-ab-20260626-184445` stayed hash-clean with zero tail-page proof/trace lines, but still lost speed: n128 baseline/candidate 46.47/45.98 tok/s, n512 36.45/35.43 tok/s. A tested sticky-route candidate preserved hashes but added zero skip coverage (`all_bound_sticky_route=0`) and was removed. Post-age-out demotion: GPU proof `qblock-ageout-trace-n128-7730-7770` showed 122 `MTP_QBLOCK_TAIL_PAGE_AGE_OUT status=aged_out` markers and actual poisoned canonical-read sum 3714, so bounded all-bound evidence is not persistent page authority. The guarded no-unsafe rerun `/qblock-paged-attention-all-bound-exclusive-clean-ab-20260627-002009` is hash-clean with `EXCLUSIVE_ALL_BOUND=1` alone: n128 `accabc9a`, n512 `8d10ba2d`, zero tail-page proof/trace lines. The trace rerun `/qblock-paged-attention-all-bound-exclusive-clean-ab-20260627-002207` preserves n128 `accabc9a` and emits 178 `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_EXCLUSIVE` lines, including 6 `reason=all_bound_unsafe_gate_disabled` lines with `all_bound_env=1 all_bound_unsafe=0 all_bound_no_miss=0`. Canonical write-through remains required until commit-back or a persistent authority table covers all attendable owned rows. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_EXCLUSIVE_ALL_BOUND_UNSAFE` | `0` | unsafe archaeology key | packed16/V4_144 FA backend | Second key required, in addition to `...EXCLUSIVE_ALL_BOUND=1`, to reproduce the old all-bound no-write-through skip behavior. It is intentionally named unsafe because bounded tail-page coverage does not protect rows after age-out. Do not use for promotion or production; use only for controlled archaeology/hash experiments. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_DEFERRED_PROOF` | `0` | proof-only diagnostic/probe | packed16/V4_144 FA backend | Records owned K/V write attempts and later resolves them when route binds or no-map high-water updates make the downstream consumer proof observable. It emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_DEFERRED_PROOF` summary lines with `would_skip_k`/`would_skip_v` and blocker counters, but it never changes canonical write-through and does not authorize the exclusive gate. This is the safe bridge for proving whether same-map route-complete + pair readiness + physical-page match + high-water would have allowed no-write-through after the fact, before any future early-skip design is considered. Host canary `test_owned_exclusive_proof_contract` covers predicate order and K-before-V. GPU proof-only gates are hash-clean but not promotable: n128 `qblock-paged-attention-deferred-proof-n128-20260626-131013` hash `accabc9a`, records 178, resolves 160, `would_skip_k/v=0/0`, max high-water 0, blocked by no high-water/route-incomplete; n512 `qblock-paged-attention-deferred-proof-n512-20260626-131107` hash `8d10ba2d`, records 392, resolves 401, `would_skip_k/v=0/0`, max high-water 8448 but observed owned attempts remain before that boundary, so blocked by `before_route_highwater`/no-high-water/route-incomplete. Follow-up instrumentation adds proof-only `route_ready_k/v`, `no_highwater_k/v`, `pre_highwater_k/v`, `post_highwater_k/v`, `op=highwater_update`, and `op=highwater_reset` fields; these fields still do not feed `OWNED_TAIL_WRITE_EXCLUSIVE`. Counter rerun remains hash-clean but blocked: n128 `qblock-paged-attention-deferred-proof-counters-n128-20260626-133856` hash `accabc9a`, route-ready K/V 17/17, no-highwater 17/17, post-highwater 0/0, highwater updates/resets 27/16; n512 `qblock-paged-attention-deferred-proof-counters-n512-20260626-133954` hash `8d10ba2d`, route-ready K/V 17/17, no-highwater 17/17, pre-highwater 1/1, post-highwater 0/0, updates/resets 128/116. This proves the next blocker is lifecycle/order: observed high-water is reset before most route-ready owned attempts, and the only surviving boundary in n512 is above the owned write range. Follow-up retired-highwater instrumentation snapshots the last nonzero high-water on data-invalidating reset and logs `retired_valid`, `retired_highwater`, `retired_generation`, `retired_reset_count`, `retired_pre_highwater_k/v`, and `retired_post_highwater_k/v`; it is historical proof only and is never used for exclusive authorization because the reset invalidated the live safety boundary. Retired rerun remains hash-clean but non-promotable: n128 `qblock-paged-attention-deferred-proof-counters-n128-20260626-140809` hash `accabc9a`, updates/resets 27/16, max retired high-water 7936, live post K/V 0/0, retired post K/V 0/0; n512 `qblock-paged-attention-deferred-proof-counters-n512-20260626-140845` hash `8d10ba2d`, updates/resets 128/116, max retired high-water 8448, live post K/V 0/0, one historical retired-post pair but final route-ready K/V are retired-pre 17/17. Follow-up hook `trigger=pre_lifecycle_reset_current_highwater` resolves attempts immediately before lifecycle reset clears ready/route state and the live high-water, so the proof can distinguish a real current-boundary `would_skip` from retired historical evidence; it is logging only and still never authorizes `OWNED_TAIL_WRITE_EXCLUSIVE`. Pre-reset rerun remains hash-clean but blocked: n128 `qblock-paged-attention-deferred-proof-counters-n128-20260626-142226` hash `accabc9a`, 9 pre-reset resolves, max route-ready K/V 16/16, no current high-water in all pre-reset resolves, would-skip 0/0; n512 `qblock-paged-attention-deferred-proof-counters-n512-20260626-142258` hash `8d10ba2d`, 74 pre-reset resolves, max route-ready K/V 17/17, one live pre-highwater pair, live post K/V 0/0, would-skip 0/0. Follow-up range instrumentation adds proof-only `cross_highwater_k/v` and `post_highwater_tokens_k/v` fields so a write that begins before the current high-water but extends past it is no longer hidden in the base-only `pre_highwater` bucket. These counters are diagnostic only and do not feed the base predicate or exclusive authorization. Range rerun remains hash-clean and confirms no hidden post-boundary rows: n128 `qblock-paged-attention-deferred-proof-counters-n128-20260626-143932` hash `accabc9a`, cross K/V 0/0, post-highwater tokens K/V 0/0; n512 `qblock-paged-attention-deferred-proof-counters-n512-20260626-144006` hash `8d10ba2d`, cross K/V 0/0, post-highwater tokens K/V 0/0. Follow-up writer-order instrumentation adds proof-only record-time fields (`record_seq`, `record_highwater`, `record_highwater_update_seq`, `record_route_complete`, `record_k_ready/v_ready`) plus resolve aggregates (`record_no_highwater_k/v`, `record_post_highwater_k/v`, `record_no_to_live_highwater_k/v`) to prove whether attempts are recorded before live high-water exists; it is logging only and does not alter readiness, base predicates, exclusive authorization, or canonical write-through. Writer-order reruns remain hash-clean and blocked. n128 `qblock-paged-attention-deferred-proof-counters-n128-20260626-150846` hash `accabc9a` shows 178/178 records captured `record_highwater=0`; highwater updates 27/27 still had `pending_attempts=0`, record/live post/cross K/V 0/0. Paired n512 `qblock-paged-attention-deferred-proof-counters-n512-20260626-151419` hash `8d10ba2d` shows 390/392 records captured `record_highwater=0`; the only 2 highwater-aware records were K/V for `[8194,8199)` below highwater 8448 with `record_route_complete=0`; record/live post/cross K/V remained 0/0. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_PLAN_PROOF` | `0` | proof-only diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_PLAN_PROOF` markers for the AITER-style plan-first safety audit. Requires paged attention plus owned-tail write envs. It snapshots the route-expected map as a `plan_id` with shared event sequencing, records K/V writer attempts with `plan_ready`, `plan_before_record`, write-through/owned-write/exclusive-skip plan bits, planned page bases, expected page coverage, and emits resolve counters for planned K/V attempts. Current proof fields also report `plan_expected_layer_pages`, `plan_missing_layer_pages`, `plan_bound_before_record`, `plan_all_expected_known`, `plan_bound_at_resolve`, `plan_known_no_miss`, and ordered `plan_bound_no_miss_would_skip_k/v` so a future plan-first exclusive gate can be evaluated without changing canonical writes. It also emits `op=writer_authority` for K/V writer attempts with `writer_from_map`, `legacy_fallback_owned`, `map_generation`, `physical_page_from_map`, `overlay_page_base`, `idx0`, and `n_tokens`, proving whether the writer destination came from the pre-published owned map or from the old scratch fallback. It also requests dispatch-bind recording so route-complete coverage can be correlated with the plan. This is logging only: it does not change K/V writes, route-complete predicates, exclusive authorization, or canonical write-through. Host checks PASS: `git diff --check`, `test-pdmq-k-formats` build/run, `llama-server` link. GPU proof runs PASS and preserve hashes: n128 `/qblock-paged-attention-plan-proof-n128-20260626-154027` hash `accabc9a`, plan records K/V 89/89, `plan_ready=178/178`, `plan_before_record=178/178`, `plan_owned_write=178/178`, `plan_exclusive_skip=0`, record highwater nonzero 0; n512 `/qblock-paged-attention-plan-proof-n512-20260626-154120` hash `8d10ba2d`, plan records K/V 196/196, `plan_ready=392/392`, `plan_before_record=392/392`, `plan_owned_write=392/392`, `plan_exclusive_skip=0`, record highwater nonzero 2. The two highwater-aware n512 records remain K/V `[8194,8199)` below highwater 8448 with `plan_route_complete=0`; no production skip authorization. Follow-up snapshot+plan proof is hash-clean and no-map clean: n128 `/qblock-paged-attention-plan-snapshot-proof-n128-20260626-191116` hash `accabc9a`, no-map0, max `plan_known_no_miss` K/V 17/17, max `plan_bound_no_miss_would_skip` K/V 16/16, but only 6 records had all layer-pages bound before writer; n512 `/qblock-paged-attention-plan-snapshot-proof-n512-20260626-191150` hash `8d10ba2d`, no-map0, same max 17/17 and 16/16, only 8 records bound before writer. A real plan-bound skip candidate was tested and removed: n128 `/qblock-paged-attention-plan-bound-exclusive-trace-n128-20260626-191836` hash-wrong `a793bef2` after plan-bound skips K/V 74/74; n512 `/qblock-paged-attention-plan-bound-exclusive-trace-n512-20260626-191907` hash-wrong `be9bee6b` after plan-bound skips K/V 205/205. Page-authority writer proof passes with canonical writes unchanged: n128 `/qblock-paged-attention-plan-proof-n128-20260626-195958` hash `accabc9a`, `writer_from_map` 178/178, `legacy_fallback_owned` 0/178, writer/consumer/dispatch generation intersection 25; n512 `/qblock-paged-attention-plan-proof-n512-20260626-200034` hash `8d10ba2d`, `writer_from_map` 392/392, `legacy_fallback_owned` 0/392, generation intersection 115. Combined owned-snapshot + writer-authority proof also passes and eliminates no-map consumers: n128 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n128-20260626-200418` hash `accabc9a`, consumer no-map 0, `writer_from_map` 178/178, fallback0, generation intersection 25; n512 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n512-20260626-200458` hash `8d10ba2d`, no-map0, `writer_from_map` 406/406, fallback0, generation intersection 122. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_LIVE_WINDOW_PROOF` | `0` | proof-only diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_TXN_TAIL_PAGE_OWNED_LIVE_WINDOW_PROOF` markers for the live safety-window audit. Requires paged attention plus owned-tail write envs. It records proof-only highwater event sequencing, route-complete event sequencing, record-time `after_highwater` / `after_route_complete` / `post_highwater` buckets, lifecycle reset closure, all-bound/no-miss `op=all_bound_resolve` counters, and resolve counters for K/V attempts that happen after both live boundaries. It also requests dispatch-bind recording. This is logging only: it does not change K/V writes, highwater semantics, route-complete predicates, exclusive authorization, or canonical write-through, and it does not feed retired highwater into any production gate. Host checks PASS: `git diff --check`, `test-pdmq-k-formats` build/run, `llama-server` link. GPU proof runs PASS and preserve hashes: n128 `/qblock-paged-attention-live-window-proof-n128-20260626-163329` hash `accabc9a`, live-window records 178, highwater events 27, route-complete events 26, records after highwater 0, after route-complete 6, after both 0, candidates/would-skip 0/0; n512 `/qblock-paged-attention-live-window-proof-n512-20260626-163405` hash `8d10ba2d`, live-window records 392, highwater events 128, route-complete events 117, records after highwater 2, after route-complete 8, after both 0, candidates/would-skip 0/0. This proves highwater and route-complete are currently disjoint at K/V record time; no production skip authorization. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PRODUCER_SNAPSHOT_OWNED_PROOF` | `0` | proof-only diagnostic/probe | host map registration + packed16/V4_144 registry | Requires `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PRODUCER_STATE_IMPORT=1`. Extends producer snapshot preservation to owned-tail-write maps on data-invalidating clears and lets the packed16 map getter fall back to an accepted published/snapshot map when the K view has no direct per-entry map, so the test can prove whether `reason=no_registered_map` highwater gaps are caused by losing the owned map snapshot. It remains default-off and hash-gated; it does not enable exclusive write skipping or alter canonical K/V write-through by itself. GPU proof after the fallback is hash-clean and positive for all-bound/no-miss: n128 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n128-20260626-170051` hash `accabc9a`, consumer no-map 292→0, bound/ok 449/449, all-bound resolves 187 with status `all_bound_would_skip` 127, max K/V 17/17; n512 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n512-20260626-170130` hash `8d10ba2d`, consumer no-map 506→0, bound/ok 922/922, all-bound resolves 556 with status `all_bound_would_skip` 481, max K/V 17/17. This proof remains logging-only; the separate `EXCLUSIVE_ALL_BOUND` gate is the first default-off no-write-through consumer of that evidence. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION_OWNED_TAIL_WRITE_EXCLUSIVE_TRACE` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Emits owned-exclusive decision lines even without registry/readiness tracing. Registry trace and readiness trace also enable these lines. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE` | `0` | guarded prototype gate | packed16/V4_144 FA backend | Enables the first real producer-side tail-page data path for packed16 K and V4_144 V: eligible non-capture, contiguous, one-page partial writes (`2..8` rows, copied backend indices, nonzero merge slots) allocate a 16-token scratch page, copy the existing destination page using the active layout, pack incoming slots into scratch, then commit the full scratch page back to the KV cache. K activation currently requires F32 source rows and packed16-Q8 K; V activation supports F32/F16 source rows and V4_144. Ineligible writes, q8 shadow K source, packed8_q4 K, full-page writes, and graph-capture probes fall back to the existing writer path. This runtime gate is silent by default; set `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PROOF=1` as well when marker evidence is needed. The server also emits `MTP_QBLOCK_TXN_TAIL_PAGE_RUNTIME` when branch transaction staging is active under the tail-page proof/request envs; that marker validates the same tail-page state ABI against the final output bundle, now also logs the host-only `tail_txn_commit_v1` descriptor/export/apply/range statuses, and reports sampler/KV/recurrent prerequisite readiness. In the combined runtime-contract proof, KV readiness is sourced from `LLAMA_MTP_QBLOCK_BRANCH_TXN_KV_SPLIT_COMMIT=1` / `MTP_QBLOCK_BRANCH_TXN_KV_SPLIT status=candidate_committed`, not from physical-import replay. `tail_page_contract_rejected` runtime lines are accepted fail-closed boundary/page-state cases when all other prerequisites are ready. Validated artifacts: descriptor-field rerun n128 `qblock-tail-page-runtime-contract-n128-20260627-041131` hash `accabc9a`, runtime `8 = 6 metadata_active + 2 contract_rejected`, descriptor status fields all OK in sampled runtime lines; descriptor-field rerun n512 `qblock-tail-page-runtime-contract-n512-20260627-041306` hash `8d10ba2d`, runtime `77 = 70 metadata_active + 7 contract_rejected`, descriptor status fields all OK in sampled runtime lines; default-off n128 `qblock-tail-page-defaultoff-guard-n128-20260624-211004` hash `accabc9a` with zero tail-page/txn markers. It reports `dispatch_active=0` unless the separate dispatch enable gate below is explicitly requested and the current runtime row has a real matching dispatch-side bind plus authoritative non-scratch bytes. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_CONSUMER` | `0` | guarded prototype gate | packed16/V4_144 FA backend | Enables the production read-side logical→physical tail-page line in packed16 DOT4/PDMQ FA. It binds when a validated `ggml_cuda_mtp_qblock_tail_page_map_v1` has been registered for the active K view, packed16-Q8 K sidecar and direct V4_144 V are active, the page size is 16 tokens, the visible FA range covers the logical tail, and the map fits the sidecar capacity. Binding no longer requires QBlock row/tree mode: non-QBlock PDMQ programs can use the page table while leaving `qblock_program.enabled=0`, so row/head/mask semantics remain unchanged. A real FA dispatch bind is recorded from this same predicate when dispatch/proof tracing is requested; the production hot path itself remains the page-table address translation and does not require the server branch-transaction proof path. The server currently publishes a canonical page-base map only after `MTP_QBLOCK_TXN_TAIL_PAGE_RUNTIME metadata_active=1` proves sampler, KV split commit, and recurrent proof/restore readiness; that metadata path depends on branch transaction commit machinery and is proof-only until a lightweight production readiness source replaces it. Failed metadata clears stale maps. Runtime lines report `consumer_requested`, `map_attempted`, `map_registered`, and `map_reason`. When a byte-authoritative committed-physical import has no same-cycle strict bind, the server records a pending proof entry and the real FA dispatch-bind recorder later emits `MTP_QBLOCK_TXN_TAIL_PAGE_DEFERRED_BIND status=resolved_exact|resolved_cover`; this is diagnostic only and never fabricates `MTP_QBLOCK_TXN_TAIL_PAGE_DISPATCH_BIND`. Latest pre-enable proof artifacts: no-scratch n128 `qblock-tail-page-consumer-activation-stateimport-noscratch-n128-20260625-185159` hash `accabc9a`, pending3/resolved3 exact2+cover1; no-scratch n512 `...n512-20260625-185403` hash `8d10ba2d`, pending25/resolved25 exact8+cover17 plus full-page-not-required3; scratch n128 `qblock-tail-page-consumer-activation-scratch-stateimport-n128-20260625-190451` hash `accabc9a`, zero deferred markers and `scratch_overlay_not_authoritative`. Without a visible registered map it emits `MTP_QBLOCK_TXN_TAIL_PAGE_CONSUMER ... bound=0 active=0 reason=no_registered_map` and leaves cache addressing unchanged; validated no-map artifact `qblock-tail-page-consumer-nomap-n128-20260625-035757` preserved hash `accabc9a` with `consumer_bound=0` and all consumer lines `reason=no_registered_map`. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TAIL_PAGE_LOGICAL_SLOT_AUDIT` | `0` | proof-only diagnostic/probe | packed16/V4_144 FA backend | Emits one `MTP_QBLOCK_LOGICAL_SLOT_AUDIT` marker per FA consumer when page attention is requested or a map is registered. The marker summarizes JetSpec-style logical→physical slot remapping from the authoritative page table: logical base/tail, table pages, first/last/min/max physical slot, canonical-token count, remapped-token count, visible-noncanonical count, and bad-slot count. It does not change bind decisions, kernel addressing, K/V writes, or exclusive authorization; it only proves what the existing `dp16_fa_qblock_tail_page_physical_k()` consumer would address. GPU proof with require-map passed: n128 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n128-20260626-203938` hash `accabc9a`, audit449/449 bound, samples9950, canonical7680, remap2270, visible-noncanonical0, bad0; n512 `/qblock-paged-attention-live-window-owned-snapshot-tailpage-n512-20260626-204037` hash `8d10ba2d`, audit922/922 bound, samples38206, canonical30384, remap7822, visible-noncanonical0, bad0. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_DISPATCH_ENABLE` | `0` | guarded proof gate | server runtime proof | Allows `MTP_QBLOCK_TXN_TAIL_PAGE_RUNTIME dispatch_active=1` only when the current row already has `metadata_active=1`, a registered consumer map, a real strict FA-side dispatch bind that exactly matches the map, and authoritative non-scratch bytes. It adds `dispatch_enable_requested` and `dispatch_enable_reason`; rows with committed-physical imports that are still waiting for the next real FA bind stay `dispatch_active=0 dispatch_enable_reason=dispatch_pending_after_committed_physical_import`, full-page-not-required rows stay inactive, and scratch rows stay blocked by `scratch_overlay_not_authoritative`. Default off; no synthetic dispatch bind is ever emitted. Enablement artifacts: no-scratch n128 `qblock-tail-page-consumer-activation-stateimport-noscratch-n128-20260625-233050` hash `accabc9a`, active3/pending3/resolved3; no-scratch n512 `...n512-20260625-233300` hash `8d10ba2d`, active42/pending25/resolved25/full-page-not-required3; scratch n128 `qblock-tail-page-consumer-activation-scratch-stateimport-n128-20260625-234358` hash `accabc9a`, active0, scratch byte authority blocked. Speed gate failed: clean n128 production baseline `qblock-tail-page-speed-clean-ab-n128-20260626-000525/prod_baseline` was 49.03 tok/s, while branch commit only `...001131/branch_commit_only` was 2.05 tok/s and tail-page minimal off/on was 2.04/2.06 tok/s; slowdown is branch transaction commit metadata, not the dispatch-enable bit. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_REGISTRY_TRACE` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Emits `MTP_QBLOCK_TXN_TAIL_PAGE_REGISTRY` lines from the packed16 tail-page map registry on map publish, clear, get-miss, and lifecycle reset. It reports whether the raw K-view key was registered, whether a global producer map was active/acceptable, the sidecar format/capacity for misses, and `op=lifecycle_reset` when host data-invalidating clear resets global maps plus the no-map high-water. It also labels host-side map clears with source reasons such as `seq_rm(seq=...,p0=...,p1=...)`. This is diagnostic evidence for separating pointer-key misses from post-publication clears; it does not alter map lookup or dispatch. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PRESERVE_SEQ_RM_DISJOINT` | `0` | guarded prototype gate | host KV cache map lifetime | Keeps a currently registered tail-page map across `seq_rm()` only when the removed logical range is provably disjoint from every active map range (`p0 >= logical_base + valid_tail` for the common cleanup-to-end case). When producer-state import or `GGML_CUDA_ROCM_MTP_QBLOCK_PAGED_ATTENTION` is enabled, non-scratch maps are preserved for this disjoint case without requiring the scratch-oriented env; scratch-overlay maps still require this explicit gate. Overlapping removals and caches with no active map still clear fail-closed unless the suffix-shrink subgate below applies. This remains a route-coverage experiment for scratch write-through; default off for scratch until exclusive scratch/txn-owned pages pass hashes and speed gates. Registry-trace proof: n128 write-through preserved hash and raised active consumers 91→154 (`qblock-tail-page-consumer-scratch-preserve-n128-20260625-043339`). |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_SHRINK_SEQ_RM_SUFFIX` | `0` | guarded prototype gate | host KV cache map lifetime | Under `...PRESERVE_SEQ_RM_DISJOINT=1`, converts suffix cleanup inside one active map from clear to metadata shrink when `seq_rm(p0, -1)` starts strictly inside the active tail map. The republished map keeps `[logical_base, p0)` and drops removed suffix rows. Conflicting maps or non-suffix overlap still clear fail-closed. Registry-trace proof: write-through n128 `qblock-tail-page-consumer-scratch-preserve-shrink-n128-20260625-045121` hash `accabc9a`, active174/no-map272; n512 `...n512-20260625-045241` hash `8d10ba2d`, active626/no-map272. Exclusive scratch with this still failed n128 (`qblock-tail-page-consumer-scratchexcl-preserve-shrink-min4096-n128-20260625-045159`, hash `a793bef2`), so this is not a promotion. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PRESERVE_SEQ_RM_SHARED` | `0` | diagnostic/probe | host KV cache map lifetime | Extra fail-closed overlap-preserve probe: before a `seq_rm(seq_id,p0,-1)` mutates KV cell metadata, it preserves a scratch-overlay map only if every overlapped mapped logical position still has a surviving owner after removing `seq_id` (`seq_count > 1` for cells containing `seq_id`, or another cell at that position without `seq_id`). It emits `op=host_preserve_clear_owner_shared` when it fires. Latest all-exclusive n128 with this gate plus the V published-map guard (`qblock-tail-page-consumer-shared-preserve-scratchexcl-n128-20260625-054138`) still failed hash `a793bef2`, because the early base0 scratch-only map is cleared by a later data clear before prefix FA reads canonical rows. Diagnostic only. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PERSIST_METADATA_CLEAR` | `0` | diagnostic/probe | host KV cache map lifetime | Preserves a single non-conflicting scratch-overlay map across `llama_kv_cache::clear(data=false)` metadata clears only. It does not survive data-invalidating clears and does not change hard clears from seq copy/import/add/div/keep, shift/copy, state load, or direct transaction rollback. In current prefix-window artifacts the blocking clear is `clear_data`, so this gate alone did not change n128 all-exclusive (`qblock-tail-page-consumer-persistmeta-scratchexcl-n128-20260625-055642`, still `a793bef2`, active159/no-map272). |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PERSIST_DATA_CLEAR_UNSAFE` | `0` | retired unsafe diagnostic/probe | host KV cache map lifetime | Historical negative probe. It formerly preserved a scratch-overlay map across `clear(data=true)` to test whether data clear was merely a coarse host invalidation; artifact `qblock-tail-page-consumer-persistdata-unsafe-scratchexcl-n128-20260625-060006` bound active431/no-map0 but still failed `a793bef2`, proving persisted base0 maps route stale/wrong scratch bytes after data clear. The current lifecycle rejects this preserve path: data-invalidating clear resets global maps, per-entry maps, cache-owned paged state, and the no-map high-water; registry trace emits `op=host_preserve_data_clear_unsafe_rejected` if this env is set. Persistent registry alone is not a fix; pages must be repopulated after data clears or canonical write-through must remain. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_PRODUCER_MAP` | `0` | guarded prototype gate | packed16/V4_144 FA backend | Extra opt-in required to publish a producer-side one-page map from the active packed16-K tail-page writer. This is separate from `...TAIL_PAGE_CONSUMER` so the no-map guard remains fail-closed: consumer requested without this producer map does not bind. When enabled with `...TAIL_PAGE=1` and `...TAIL_PAGE_CONSUMER=1`, eligible producer writes publish the current page-base/valid-tail map immediately before matching FA consumers, allowing `MTP_QBLOCK_TXN_TAIL_PAGE_CONSUMER ... bound=1 active=1 reason=ok`. Validated final-build artifacts: producer-only n128 `qblock-tail-page-consumer-produceronly-n128-20260625-035935` hash `accabc9a`, n512 `qblock-tail-page-consumer-produceronly-n512-20260625-040004` hash `8d10ba2d`; both keep `dispatch_active=0` and are speed-neutral vs default-off. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_SCRATCH_MAP` | `0` | guarded prototype gate | packed16/V4_144 FA backend | Optional producer-map subgate for the active tail-page writer. Eligible partial K/V writes still update the canonical contiguous rows for no-map consumers, then mirror the same rows into a spare physical page and publish a `SCRATCH_OVERLAY` logical-tail map for page-table consumers. This bypasses the full 16-token page scratch copy/merge/commit path but is intentionally write-through until map coverage is complete. Exclusive scratch-overlay writes were hash-wrong (`a793bef2`); write-through plus non-QBlock PDMQ binding restored hashes and raised traced scratch coverage from 32 to 91 active consumers: clean n128 `qblock-tail-page-consumer-scratch-n128-20260625-040726` hash `accabc9a`, clean n512 `qblock-tail-page-consumer-scratch-n512-20260625-040639` hash `8d10ba2d`, traced n128 `qblock-tail-page-consumer-scratchtrace-n128-20260625-040610` hash `accabc9a` with `consumer_active=91`. Default off; not a production promotion or `dispatch_active=1` claim. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_SCRATCH_EXCLUSIVE` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Subgate under `...SCRATCH_MAP=1` that can skip canonical K/V writes and write only the spare physical scratch page. It now fails closed unless `...SCRATCH_EXCLUSIVE_AFTER_NOMAP_MAX=1` proves the write starts after the observed unmapped prefix-scan high-water, or the explicitly unsafe `...SCRATCH_EXCLUSIVE_SINGLE_MAP_UNSAFE=1` override is set. This matches the vLLM lesson: without a full logical-token→physical-page table, a single scratch-overlay map is not route-complete and must keep canonical write-through. V exclusive is additionally fail-closed on a currently published K scratch map covering the same logical range. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_SCRATCH_EXCLUSIVE_MIN_IDX` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Optional lower bound for the exclusive scratch diagnostic. It no longer makes single-map exclusive writes active by itself; pair with `...SCRATCH_EXCLUSIVE_AFTER_NOMAP_MAX=1` for the observed safe high-water probe, or with `...SCRATCH_EXCLUSIVE_SINGLE_MAP_UNSAFE=1` only to reproduce stale/single-map failures. Historical evidence: min4096 was too early and failed `a793bef2`; min7936/high-water was hash-safe because it skipped only after the unmapped prefix scan. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_SCRATCH_EXCLUSIVE_AFTER_NOMAP_MAX` | `0` | diagnostic/probe | packed16/V4_144 FA backend | Route-complete guard for `...SCRATCH_EXCLUSIVE=1`: the backend records the largest `nk` seen by a `reason=no_registered_map` consumer and allows exclusive scratch writes only when the write's first logical index is at or beyond that observed high-water. This replaces hard-coded min7936-style probes with an observed prefix-window boundary while staying fail-closed before any no-map range has been seen. Data-invalidating clear resets the high-water to 0 so a stale prefix observation cannot authorize exclusive scratch after cache bytes are cleared. It is not a production control; it only proves whether post-prefix/decode-tail exclusive writes are separable from unmapped historical-prefix FA scans. Revalidated after the V published-map guard: n128 `qblock-tail-page-consumer-oracle-scratchexcl-after-nomap-n128-20260625-060238` hash `accabc9a`, active174/no-map272, 48.37 tok/s; n512 `...after-nomap-n512-20260625-054330` hash `8d10ba2d`, active604/no-map272, 39.25 tok/s. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_TXN_TAIL_PAGE_SCRATCH_EXCLUSIVE_SINGLE_MAP_UNSAFE` | `0` | unsafe diagnostic/probe | packed16/V4_144 FA backend | Restores the old single-map exclusive behavior for negative reproduction only. With a single scratch page/map, later prefix FA cannot see all historical exclusive rows; if data clears are forced to preserve maps, all consumers may bind yet still hash `a793bef2` due stale/wrong scratch bytes. Do not use for promotion. |
| `LLAMA_MTP_QBLOCK_BRANCH_NEXTCYCLE_CACHE_COMPARE` | `0` | diagnostic/probe | server sampler/spec state | When transaction replay compare has captured a sampler-oracle branch replay bundle, caches that bundle until the next ordinary cycle and emits `MTP_QBLOCK_BRANCH_NEXTCYCLE_CACHE_COMPARE` before any next target decode. The marker checks whether the current production `id_last` matches the cached branch final token and, for token-matched bundles, compares the cached branch replay recurrent-state bytes with the live production prefix-state bytes. It logs raw hash/first-diff fields plus canonical hashes/first-diff fields that zero the top-level serialized `seq_id` header, and prints small hex windows around raw/canonical diffs. It is metadata/read-only, performs no scratch decode or seq copy, reports prefix/token/state mismatches as evidence, and hard-codes `production_mutation=0`, `same_cycle_replayable=0`, and `safe_commit=0`. Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_BRANCH_STATE_SEGMENT_COMPARE` | `0` | diagnostic/probe | server sampler/spec state | When nextcycle cache compare has a prefix/token-matched sampled replay and live state, emits `MTP_QBLOCK_BRANCH_STATE_SEGMENT_COMPARE`. It parses the partial recurrent sequence-state stream, reports cell counts and first cell positions, computes a second canonical digest that zeroes both the top-level serialized `seq_id` and recurrent cell `pos` metadata, and reports the first mismatching recurrent payload component (`R`/`S` layer). It also captures a boundary prefix state just before the sampled replay's final token and compares that to live pre-target state, via `boundary_prefix_*` fields, to distinguish simple one-token staging mismatch from earlier replay/commit divergence. This is the layer64-scratch lesson applied to state bytes without mutating production. Default off; clean production gates must leave it disabled. |
| `LLAMA_MTP_QBLOCK_SIBLING_TARGET_ROWS_SCRATCH_N_SEQ` | unset | diagnostic/probe | target context init | Raises target-context `n_seq_max` above server slot count for scratch-row proofs. The 9B target-row proof runner uses `2` with `--parallel 1`, `--kv-unified`, and doubled `--ctx-size` so production remains slot/seq `0` while scratch replay uses seq `1` in the same KV stream. |
| `LLAMA_MTP_QBLOCK_SIBLING_BRANCH_REPLAY_TRACE` | `0` | diagnostic/probe | server sampler/spec state | Emits `MTP_QBLOCK_BRANCH_REPLAY` after a sibling-hit reject resolves through normal final commit, and if a next normal draft is armed. It records whether the sibling sampled token is present in the emitted commit bundle, `output_tail_after_sampled` token-list fields, and the fail-closed transaction contract for replacing that tail (`transactional_*_required`, target branch rows, draft descendants). Same-cycle replay stays disabled (`same_cycle_replayable=0`, `safe_commit=0`). Branch-desc tracing enables this trace automatically. |

Sibling-row proof runs should use `scripts/hip/run-mtp-qblock-sibling-proof.py`. The proof requires `tree_mask=1`, `ATTENTION_ONLY` sidecar route metadata, `sidecar_in_spec_i_batch=0`, sidecar trimming before `common_speculative_process()`, replay from checkpoint of only sampled + accepted selected-path tokens, and final output with `sidecar_in_ids=0` / `sidecar_in_output=0`. A clean production gate must still run separately with these diagnostic envs disabled. The safe first top1-hit probe uses the 9B runner `scripts/hip/run-mtp-qblock-sibling-logits-probe-9b.py`; it is diagnostic only, emits parent-logit ranks, branch-plan preconditions, branch-state readiness, sampled-sibling root/subtree capture status, proof-only sampled-sibling descendant capture status, proof-only target branch row capture status, and normal next-cycle branch replay status; it must not be used as a speed gate.

## Standard route controls

| Env | Default | Status | Scope | Notes |
| --- | --- | --- | --- | --- |
| `LLAMA_MTP_ENABLE_FA` | `1` | standard | MTP graph | MTP FA route family is enabled by default when flash-attn is active; set `LLAMA_MTP_ENABLE_FA=0`, `LLAMA_MTP_DISABLE_FA=1`, or `GGML_CUDA_ROCM_MTP_DISABLE_FA=1` for controls. |
| `LLAMA_MTP_DISABLE_PACKED16_FA` | `0` | standard | MTP graph | Packed16 FA/QBlock route is enabled by default; set `1` to fall back. |
| `LLAMA_MTP_QBLOCK_TARGET_VERIFY_ACTIVE` | server-scoped | internal/standard | server → graph/backend | Server RAII signal set only around target-verifier decode chunks that contain only verifier rows. This is the production QBlock activation signal; do not export manually or use as a candidate gate. |
| `LLAMA_MTP_QBLOCK_ACTIVE` | unset | legacy/probe-only | MTP graph/probes | Legacy global signal retained for MTP graph/probe paths only. Ordinary decoder graphs ignore it to avoid globally misclassifying unrelated prefill/decode work. Do not use for production speed gates. |
| `LLAMA_MTP_QBLOCK_FUSED_VERIFY` | `0` | proof/opt-in | Qwen35 verifier graph | Enables the first-class fused QBlock/PDMQ verifier route for the non-recurrent tail layer when paired with `LLAMA_MTP_QBLOCK_FUSED_VERIFY_TAIL_LAYER=63`. Replaces deprecated `LLAMA_MTP_QBLOCK_PREFIX_FUSED_VERIFY`. |
| `LLAMA_MTP_QBLOCK_FUSED_VERIFY_TAIL_LAYER` | unset | proof/opt-in | Qwen35 verifier graph | Tail layer selector for fused QBlock/PDMQ verifier proof runs; Qwen35 target trunk tail is `63`. Replaces deprecated `LLAMA_MTP_QBLOCK_PREFIX_FUSED_VERIFY_TAIL_LAYER`. |
| `LLAMA_MTP_QBLOCK_FUSED_VERIFY_LAYER_MIN/MAX` | unset | unsafe prototype filter | Qwen35 verifier graph | Layer-major prototype filter. Keep unset for tail-fused proof route; aliases deprecated `LLAMA_MTP_QBLOCK_PREFIX_FUSED_VERIFY_LAYER_MIN/MAX`. |
| `LLAMA_MTP_QBLOCK_FUSED_VERIFY_LAYER_MAJOR_UNSAFE` | `0` | unsafe prototype | Qwen35 verifier graph | Explicit gate for the old layer-major prototype. Do not use for promotion evidence. Alias: deprecated `LLAMA_MTP_QBLOCK_PREFIX_FUSED_VERIFY_LAYER_MAJOR_UNSAFE`. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX` | `1` | standard | server scheduler | Selects QBlock recurrent-prefix verifier backend when recurrent state requires exact prefix verification; set `0` to fall back to serial/recurrent-prefix alternatives. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX_TOKEN_MAJOR_CANDIDATE` | `0` | failed candidate | Qwen35 prefix graph | Default-off token-major recurrent-prefix batching candidate. Strict n32 failed pre-repair state parity; kept only for diagnostics, not promotion evidence. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX_TOKEN_MAJOR_CANDIDATE_LAYER` | unset | failed candidate filter | Qwen35 prefix graph | Optional single-layer filter for token-major recurrent-prefix candidate. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX_TOKEN_MAJOR_CANDIDATE_LAYERS` | unset | failed candidate filter | Qwen35 prefix graph | Optional comma/range layer filter for token-major recurrent-prefix candidate; overrides the single-layer filter. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX_CONV_BATCH_CANDIDATE` | `0` | candidate | Qwen35 prefix graph | Default-off conv-batched recurrent-prefix candidate. Batches recurrent input conv/R snapshots but keeps GDN/S state updates row-serial AR; requires strict compare and speed A/B before promotion. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX_CONV_BATCH_CANDIDATE_LAYER` | unset | candidate filter | Qwen35 prefix graph | Optional single-layer filter for conv-batched recurrent-prefix candidate. |
| `LLAMA_MTP_QBLOCK_RECURRENT_PREFIX_CONV_BATCH_CANDIDATE_LAYERS` | unset | candidate filter | Qwen35 prefix graph | Optional comma/range layer filter for conv-batched recurrent-prefix candidate; overrides the single-layer filter. |
| `LLAMA_MTP_QBLOCK_PREFIX_FULL_ATTN_BATCH` | `1` | standard | Qwen35 prefix graph | Enables QBlock full-attn prefix batching container; set `0` to fall back. Layer filters remain available for experiments. |
| `LLAMA_MTP_QBLOCK_PREFIX_FULL_ATTN_SERIAL_QKV_BATCHED_FA` | `1` | standard | Qwen35 prefix graph | Standard exact QBlock FA path. Unset default applies to validated non-terminal full-attn layers; explicit `=1` with no layer filter keeps legacy all-layer opt-in behavior; set `0` to fall back. |
| `LLAMA_MTP_QBLOCK_PREFIX_FULL_ATTN_SERIAL_QKV_BATCHED_FA_LAYERS` | unset | route override | Qwen35 prefix graph | Optional list/ranges; overrides the standard non-terminal layer policy for serial-QKV batched FA. |
| `LLAMA_MTP_QBLOCK_STRICT` | off unless requested | strict | graph/backend | Strict QBlock route gate. Diagnostic/assertion mode, not standard runtime default. Allows row-serial pre-tail layers during fused-tail verifier proof runs; the fused block itself must still satisfy QBlock invariants. |
| `LLAMA_MTP_VERIFY_COMPARE` | off unless requested | strict | verifier | Required for strict compare runs. Diagnostic; do not enable for normal standard route. |
| `LLAMA_MTP_QBLOCK_BLOCK_VERIFY_TRACE` | `0` | diagnostic/proof | server scheduler | Emits compact block-verifier target-decode lines with backend, draft rows, batch rows, forced accepted cap, sampled token, and draft token list. Used by `scripts/hip/run-mtp-qblock-nmax-probe.py`. |
| `LLAMA_MTP_QBLOCK_BLOCK_VERIFY_FORCE_ACCEPTED_ROWS` | unset | diagnostic/proof | server scheduler | Caps accepted draft rows after target sampling and applies the same cap to token oracle. Use only to force partial-accept/rollback cases; not a production knob. Accepted-row state proof is `MTP_VERIFY_COMPARE_POST_PREFIX_COMMIT` when prefix commit is enabled; per-token serial replay is logged separately as diagnostic because multi-token recurrent prefix arithmetic is not byte-identical for accepted>0. |
| `LLAMA_MTP_SPEC_CKPT_HOST` | `0` | diagnostic/proof | server scheduler | Stores speculative checkpoints in host state buffers instead of ON_DEVICE checkpoint buffers. Slow isolation knob for checkpoint load/save investigations; default remains ON_DEVICE. |
| `LLAMA_MTP_HIDDEN_SHIFT_TRACE` / `LLAMA_MTP_HIDDEN_SHIFT_REQUIRE` | `0` | diagnostic/proof | MTP draft process | Traces and optionally aborts on MTP hidden-shift invariants: pending hidden becomes catch-up row0, target verify row0 becomes catch-up row1, and verifier last hidden becomes next pending hidden. |
| `LLAMA_MTP_QBLOCK_TRACE` | off | diagnostic | graph/backend | Route/state trace. High log volume. |
| `LLAMA_MTP_QBLOCK_DISABLE` | `0` | rollback | server scheduler | Disables server-side QBlock verifier scheduling and falls back to non-QBlock verifier behavior. |
| `LLAMA_MTP_QBLOCK_RECURRENT_REPAIR` | `0` | diagnostic/repair | server scheduler | Legacy repair experiment for recurrent QBlock state; not promotion evidence and not standard runtime. |
| `LLAMA_MTP_QBLOCK_REJECT_TRACE` | `0` | diagnostic | server scheduler | Emits QBlock rejection trace telemetry, including seq/position, planned/materialized rows, accepted rows, original/chunked draft size, commit mode, backend, and first reject row. Must not change accepted state. |
| `LLAMA_MTP_QBLOCK_VERIFY_CHUNK` | `1` | standard | server scheduler | Standard QBlock verifier chunk/fallback path. When a default speculative draft is larger than the chunk size and recurrent rollback is disabled, verifies the first chunk. If the chunk fully accepts while tail remains, it restores the checkpoint and falls back to full verification; otherwise it commits/re-drafts normally. Does not change ngram/spec knobs. Set `0` to disable. |
| `LLAMA_MTP_QBLOCK_VERIFY_CHUNK_SIZE` | `4` | standard | server scheduler | Draft-token chunk size for `LLAMA_MTP_QBLOCK_VERIFY_CHUNK`; clamped to at least 1. |
| `LLAMA_MTP_QBLOCK_VERIFY_CHUNK_TRACE` | `0` | diagnostic | server scheduler | Emits QBlock verifier chunk telemetry. Must not change accepted state. |
| `LLAMA_MTP_QBLOCK_VERIFY_CHUNK_STAGED_CANDIDATE` | `0` | candidate | server scheduler | Default-off staged-tail subcandidate for QBlock verifier chunking. If a chunk boundary matches the original tail, verifies subsequent tail chunks before committing instead of discarding the tail or immediately falling back to full verification. High risk; requires long output-hash A/B before promotion. |
| `LLAMA_MTP_QBLOCK_VERIFY_CHUNK_STAGED_MAX_CHUNKS` | `16` | candidate | server scheduler | Safety cap on staged tail chunks per QBlock block. |
| `LLAMA_MTP_QBLOCK_NQ` | unset | diagnostic/shape | backend | Test-only QBlock `nq` override/reporting aid. Do not use for promotion runs unless named in artifact. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_PDMQ` | route-dependent | standard | backend | Enables PDMQ QBlock backend path. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_VEC` | `0` | candidate | backend | Enables the QBlock verify VEC candidate for packed16 K + q4_0 V. Does not replace PDMQ; requires cols=nq vs cols=1 bit-exact smoke before promotion. |
| `GGML_CUDA_ROCM_PACKED16_FA2_VEC` | `0` | experimental/global | backend | Legacy/global packed16 FA2 VEC opt-in for packed16 K + q4_0 V. Do not use as QBlock promotion evidence; prefer `GGML_CUDA_ROCM_MTP_QBLOCK_VEC` for QBlock-scoped validation. |
| `GGML_CUDA_ROCM_PACKED16_FA2_VEC_COLS` | auto | candidate/smoke | backend | Packed16 VEC columns per block. For `MTP_QBLOCK_VERIFY_QK` with `GGML_CUDA_ROCM_MTP_QBLOCK_VEC=1`, `4/8/16` are QBlock-scoped candidates and `1` is the bit-exact smoke baseline; outside QBlock, multi-col values remain unsafe probe-only. |
| `GGML_CUDA_ROCM_MTP_VERIFY_SMALLQ_PDMQ` | route-dependent | standard | backend | Small-Q PDMQ verify selection. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_MAX_NQ` | `8` | standard | graph/server/backend | QBlock max FA rows; falls back to `GGML_CUDA_ROCM_SMALL_VERIFY_MAX_NQ` when set. Graph/server stamping must fail open to regular verify above this cap, before backend route selection. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_SHAPE` | auto | shape override | backend | Explicit compact shapes: `1x32`, `2x32`, `4x32`, `8x32`, `16x16`. Artifact name must include forced shape. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_SHAPE_POLICY` | `safe` | experimental | backend | `tetris`/fit policies. Not a promotion gate alone. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_GQA_GROUP` | auto/`1` in strict GQA1 route | shape/dataflow | backend | Allowed values currently `1`, `2`, `4`, `6`. Must be artifact-visible. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_SPLITK` | `1` for non-split | experimental | backend | Split-K route is not strict-state exact unless separately proven. |
| `GGML_CUDA_ROCM_MTP_QBLOCK_SPLITK_MIN_NK` | route default | experimental | backend | Split-K threshold. |
| `GGML_CUDA_ROCM_V4_K16D16_144_MTP_QBLOCK_GQA4_AUTO` | `1` | guarded promotion | backend | Auto-selects V4/PV4 QBlock GQA4 split-K on Qwen 9B topology (`hq=16,hk=4`) only when no explicit GQA env is set and `nk >= ...AUTO_MIN_NK`. Short prompts stay GQA1. Set `0` to force legacy auto-GQA1. |
| `GGML_CUDA_ROCM_V4_K16D16_144_MTP_QBLOCK_GQA4_AUTO_MIN_NK` | `2048` | guarded promotion threshold | backend | Minimum active K tokens for the auto GQA4 QBlock route. Promotion gate: long64 nK=2048 +6.8%, exact-old pp8k 98.12 tok/s; short48 nK<=512 lost, so threshold must not be lowered without a fresh gate. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_REQUIRE_DECODE_ONLY` | `0` | diagnostic/gate hygiene | backend | Legacy attribution guard for rare runs that still set `GGML_CUDA_FA_ROUTE_REQUIRE=rocm_packed16_dot4_mmq`: pure prefill (`inst=NONE/PREFILL_QK`, `nq>1`) falls through to packed16 WMMA/DBV instead of being forced onto PDMQ. Normal V4/PV4 decode experiments should not need global route force; the selector prefers PDMQ for generated-token V4/PV4 decode when PV4/profile/staged/split envs request it. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_DECODE_STAGE_AUTO` | V4/PV4 decode auto, `0` opt-out | standard V4/PV4 decode policy | backend | Enables staged PDMQ decode/verify split policy keyed by role, `nq`, active `nk`, and V path. V4/PV4 generated-token decode now uses the measured split-K table by default when no explicit split env is set; set this to `0` or set `GGML_CUDA_ROCM_V4_K16D16_144_PDMQ_DECODE_SPLITK` explicitly to override. Raw q4/q8/f16 staged policy remains opt-in with `=1`. It uses existing PDMQ GQAX split-K kernels only and respects explicit global/QBlock split envs, `P16_STREAMK_AUTO`, and any user-set `GGML_CUDA_ROCM_V4_K16D16_144_PDMQ_DECODE_SPLITK` including `=0`. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_DECODE_STAGE_LOG` | `0` | diagnostic | backend | Emits `PDMQ_DECODE_STAGE_POLICY` with role, shape, `nq`, `nk`, V path, requested GQA group, staged shape flag, explicit split flags, and split-K request. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_DECODE_STAGE_SPLITK` | unset | explicit candidate input | backend | Forces staged PDMQ decode/verify split count when the staged policy is active and other explicit split envs are unset. Valid values follow existing PDMQ split-K values. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_DECODE_STAGE_SPLITK_NK{4096,6144,12288,24576,32768}` | policy default | candidate input | backend | Optional per-`nk` staged V4/PV4 PDMQ decode split counts. Current V4/PV4 decode table follows GPU sweep evidence, with the 9B exact-old pp8k gate updating nk≈8K from split16 to split32: prompt_n≈4001 split32, prompt_n≈7734 split32, prompt_n≈15468 split32, prompt_n≈30936 split64. 48K still needs a dedicated gate. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_DECODE_STAGE_SHAPE_NQ{1,2,4,8}` | unset | explicit shape experiment | backend | Optional staged shape override. Stage-auto never changes production PDMQ shape silently because shape is hash-sensitive; these envs are explicit experiments and must be artifact-visible. |
| `GGML_CUDA_ROCM_Q8K_DOT4_DECODE_STAGE_AUTO` | `0` | secondary default-off candidate | backend | Enables staged q8k/packed16 generated-token decode split policy. Existing split-K kernels remain unchanged; only the default split threshold and split size change when explicit split envs are unset. PDMQ remains the production V4/PV4 path. |
| `GGML_CUDA_ROCM_Q8K_DOT4_DECODE_STAGE_LOG` | `0` | diagnostic | backend | Emits `Q8K_DECODE_STAGE_POLICY` lines with implementation, `nq`, `nk`, V type, threshold, and split size when split-K decode is selected. |
| `GGML_CUDA_ROCM_Q8K_DOT4_DECODE_STAGE_AUTO_MIN_NK` | `4096` | candidate input | backend | Default split-K threshold used by q8k decode stage auto unless `GGML_CUDA_ROCM_Q8K_DOT4_DECODE_SPLITK_THRESHOLD` is explicitly set. |
| `GGML_CUDA_ROCM_Q8K_DOT4_DECODE_STAGE_AUTO_SPLIT_SIZE_NK{4096,8192,16384,32768}` | policy default | candidate input | backend | Optional q8k decode stage auto split sizes. Explicit `GGML_CUDA_ROCM_Q8K_DOT4_DECODE_SPLITK_SIZE` still wins. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_LOGICAL_K_STRIDE` | `0` | proof/probe only | backend | Forces PDMQ K sidecar indexing to compact logical rows (`head_stride=nk`). Required only for QBlock prefix/probe equivalence harnesses that compare row-local logical FA views. Do not enable for production forced-PDMQ or default MTP speed gates; production must use capacity-strided sidecar rows to preserve hash `4219d799` on the 44k step44 gate. |
| `GGML_CUDA_DP16_FA_QBLOCK_Q_PRECISION` | route default | qpack | backend | QBlock Q precision, e.g. `qpack`. |
| `LLAMA_MTP_QBLOCK_Q_PRECISION` | alias | qpack | backend | Alias for QBlock Q precision. |
| `GGML_CUDA_DP16_FA_QBLOCK_ROWMAP_MODE` | route default | qprog | backend | QBlock rowmap mode, e.g. `identity`. |
| `LLAMA_MTP_QBLOCK_ROWMAP_MODE` | alias | qprog | backend | Alias for QBlock rowmap mode. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_PVBLOCK_EXACT` | `1` | standard | backend | Exact scalar PVBlock specialization for packed16 DOT4/MMQ. Default-on when supported (`q4_0` raw-LDS V, no staged V, no competing PV candidate); set `0` to force the old scalar PV accumulator path for A/B or rollback. |
| `GGML_CUDA_ROCM_PACKED16_K_SCALE_GROUP_QBLOCKS` | route default | scale | backend | K scale grouping. Contract-sensitive. |
| `LLAMA_MTP_PACKED16_K_SCALE_GROUP_QBLOCKS` | alias | scale | backend | Alias for K scale grouping. |

### QBlock VEC cols smoke gate

Before promoting `GGML_CUDA_ROCM_MTP_QBLOCK_VEC=1` for a QBlock shape, run a bit-exact cols gate against the same request/workload:

```bash
scripts/hip/qblock-vec-cols-smoke.py --nq 4 -- <command-that-emits-sha-or-json-summary>
```

The helper runs the command with `GGML_CUDA_ROCM_PACKED16_FA2_VEC_COLS=1` and then with `GGML_CUDA_ROCM_PACKED16_FA2_VEC_COLS=<nq>`, both under `GGML_CUDA_FA_ROUTE_REQUIRE=rocm_packed16_fa2_vec`, and forces generic unsafe VEC probes off so non-QBlock VEC calls remain on their validated cols path. It fails unless both hashes match and both merged logs show the `rocm_packed16_fa2_vec` route marker. Use `--no-require-route-log` only when route evidence is checked separately. This validates the QBlock-scoped `cols=nq` promotion independently from PDMQ.

## WMMA / PDMQ diagnostics and candidates

| Env | Default | Status | Scope | Notes |
| --- | --- | --- | --- | --- |
| `GGML_CUDA_ROCM_P16_STREAMK_PLAN_LOG` | `0` | telemetry-only diagnostic | packed16 PDMQ planner | Emits `P16_STREAMK_PLAN` lines after current packed16/PDMQ split-K planning is finalized and before any kernel launch. It reports role, route, shape, `nq`/`nk`, head counts, GQA ratio/group, current split-K, occupancy-target requested/supported splits, split length, partial-byte estimates, support reason, Q stage, V path, and grid. By itself it does not change route selection, split count, allocation, kernel launch, K/V writes, outputs, hashes, or draft acceptance. |
| `GGML_CUDA_ROCM_P16_STREAMK_AUTO` | `0` | default-off candidate | packed16 PDMQ split-K scheduler | When enabled, uses the `P16_STREAMK_PLAN` supported split count for already split-capable PDMQ routes only. It does not create a new route/kernel, does not force unsupported verifier rows into split-K, and does not override explicit global `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_GQAX_SPLITK` or QBlock `GGML_CUDA_ROCM_MTP_QBLOCK_SPLITK` settings. Hash/speed gate before promotion. |
| `GGML_CUDA_ROCM_P16_STREAMK_TARGET_CTAS` | device CUs * resident target | planner input | packed16 PDMQ planner | Optional input to `P16_STREAMK_PLAN` requested-split math. Telemetry-only unless `GGML_CUDA_ROCM_P16_STREAMK_AUTO=1`. |
| `GGML_CUDA_ROCM_P16_STREAMK_RESIDENT_CTAS_PER_CU` | `1` | planner input | packed16 PDMQ planner | Optional resident-CTA multiplier used to derive default target CTAs for `P16_STREAMK_PLAN`. Telemetry-only unless `GGML_CUDA_ROCM_P16_STREAMK_AUTO=1`. |
| `GGML_CUDA_ROCM_P16_STREAMK_M_TILE` | `gqa_ratio` | planner input | packed16 PDMQ planner | Optional effective-M tile used by `P16_STREAMK_PLAN` base-CTA math. Telemetry-only unless `GGML_CUDA_ROCM_P16_STREAMK_AUTO=1`. |
| `GGML_CUDA_ROCM_P16_STREAMK_MAX_SPLITS` | `32` | planner input | packed16 PDMQ planner | Optional maximum supported split count used by `P16_STREAMK_PLAN`; clamped to `1..64`. Telemetry-only unless `GGML_CUDA_ROCM_P16_STREAMK_AUTO=1`. |
| `GGML_CUDA_ROCM_P16_STREAMK_MIN_NK` | `4096` | planner input | packed16 PDMQ planner | Optional minimum `nk` for marking the planner split as supported. Telemetry-only unless `GGML_CUDA_ROCM_P16_STREAMK_AUTO=1`. |

PDMQ phase profiling, WMMA oracle trace, WMMA QK parity, and low-level tail/state probes were removed from the live PDMQ route in the stage-1 cleanup. Use archived research notes or a new microbench/test harness for future layout/parity work.

## PWMMA reference/probe knobs

These are for packed16/PV4 PWMMA routes and packed8 prefill routing, not QBlock route promotion.

| Env | Default | Status | Notes |
| --- | --- | --- | --- |
| `GGML_CUDA_PWMMA_PROFILE` | `0` | diagnostic | PWMMA timing. |
| `GGML_CUDA_PWMMA_ABORT_AFTER_LAYOUT` | `0` | debug | Layout abort. |
| `GGML_CUDA_PWMMA_BM64_PROBE` | `0` | diagnostic | BM64 PWMMA probe. |
| `GGML_CUDA_PWMMA_DUMP_PACKED16` | `0` | diagnostic | Dump packed16 layout. |
| `GGML_CUDA_PWMMA_I8_DOT4_SHADOW_PROBE` | `0` | diagnostic | Synthetic i8 WMMA/DOT4 shadow probe. |
| `GGML_CUDA_PWMMA_I8_LIVE_DOT4_SHADOW` | `0` | diagnostic | Live tile i8 WMMA/DOT4 shadow. |
| `GGML_CUDA_PWMMA_PV_MULTI_PROBE` | `0` | diagnostic | PV multi probe. |
| `GGML_CUDA_PWMMA_PV_WMMA_SHADOW` | `0` | diagnostic | PV-WMMA shadow. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_QK` | `0` | diagnostic | Enables one-tile DBV QK/prob dump for packed16 PWMMA FA. Captures `64x16` raw QK, masked QK, probabilities, and row `m/l/alpha`; skipped during graph capture. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_LAYER` | `-1` | diagnostic | Optional layer suffix filter for packed16 FA debug QK dumps; `-1` accepts the first matching DBV node. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_QTILE` | `0` | diagnostic | Query tile index for packed16 FA debug QK dump. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_HEAD` | `0` | diagnostic | Query head index for packed16 FA debug QK dump. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_BATCH` | `0` | diagnostic | Batch index for packed16 FA debug QK dump. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_KT` | `0` | diagnostic | K tile index (`16` keys per tile) for packed16 FA debug QK dump. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_NK` | `-1` | diagnostic | Optional current-context filter for packed16 FA debug QK dump. Use e.g. `32768` to capture only the final 32k prefill chunk instead of every chunk. |
| `GGML_CUDA_ROCM_PACKED16_FA_DEBUG_DUMP` | `/tmp/packed16-fa-debug` | diagnostic | File prefix for packed16 FA debug QK dumps; writes `.qk_raw.f32`, `.qk_masked.f32`, `.probs.f32`, `.m.f32`, `.l.f32`, `.alpha.f32`, and `.meta`. |
| `LLAMA_F16_FA_KQ_MASK` | `0` | experimental opt-in | Allocates FA graph input KQ masks as F16 and writes typed FP16 mask values directly, avoiding the F32 input plus F32→F16 cast node. Intended for large `--ubatch-size` probes; default-off until MTP hash/no-NaN and VRAM gates pass. |
| `LLAMA_F16_FA_KQ_MASK_STAGE` | `1` when F16 mask is enabled | experimental control | With `LLAMA_F16_FA_KQ_MASK=1`, inserts an explicit same-type F16→F16 graph CPY before FA so ROCm consumes a normal staged tensor instead of a direct CPU/ROCm_Host graph input. This is the default opt-in behavior because unstaged F16 masks regress ROCm reserve; set `0` only for diagnostics/control. |
| `LLAMA_FA_IMPLICIT_CAUSAL_MASK` | `0` | experimental opt-in | For metadata-aware PDMQ compressed-K FA routes (packed16_q8, packed8_q4, and future PDMQ sidecar formats; DBV PWMMA or DOT4/PDMQ dispatch), drops the dense KQ mask when KV metadata proves a simple contiguous single-sequence causal tail batch. Honors `GGML_CUDA_ROCM_PDMQ_K_CACHE` and legacy `GGML_CUDA_ROCM_Q8K_DOT4_PACKED16_K_CACHE`; `GGML_CUDA_ROCM_PDMQ_K_FORMAT=none` and `GGML_CUDA_ROCM_PACKED16_DISABLE=1` keep dense masks. Passes op-param metadata `[valid_n_kv, q_offset, flags]` to the kernel, which masks padded tail/future positions internally. Falls back to the dense mask unless the proof passes. |
| `GGML_SCHED_TRACE_KQ_MASK_COPIES` | `0` | diagnostic | Logs scheduler placement/support decisions for tensors whose name contains `kq_mask`, including source/destination backend, buffer type, bytes, op, and support result. |
| `GGML_SCHED_SPLIT_LARGE_UNSUPPORTED_INPUTS_MB` | unset | experimental scheduler opt-in | If set to a positive MiB threshold, starts a fresh scheduler split at the first same-backend consumer of a user input that is larger than the threshold and unsupported by the consumer backend. Intended to shorten the live range of scheduler-created device copies for large host inputs such as F16 FA KQ masks; probe with `64` for ubatch-sized masks. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PWMMA_PREFILL` | `0` | legacy candidate | Older V4 PWMMA prefill opt-in; superseded by the PV4-named default candidate/disable pair. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4_PWMMA_PREFILL_CANDIDATE` | `1` | main candidate | PV4 V144 PWMMA prefill candidate. Default-on for non-MTP/no-spec prefill when V is `GGML_TYPE_V4_K16D16_144`; still guarded by min/max nq/nk knobs. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4_PWMMA_DISABLE` | `0` | rollback/control | Hard-disables PV4 V144 PWMMA selection even when the default candidate would select it. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4_PWMMA_PREFILL_MIN_NQ` | `16` | diagnostic/candidate scope | Minimum `nq` for the PV4 PWMMA prefill candidate. Default avoids verifier-like n4 target batches. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4_PWMMA_PREFILL_MAX_NQ` | `0` | diagnostic/candidate scope | Optional maximum `nq`; `0` means unlimited. Used to isolate tail/small-batch drift. |
| `GGML_CUDA_ROCM_V4_K16D16_144_PV4_PWMMA_PREFILL_MAX_NK` | `0` | diagnostic/candidate scope | Optional maximum `nk`; `0` means unlimited. Used to isolate prompt chunk drift. |
| `GGML_CUDA_ROCM_PACKED8_Q4_144_PWMMA_PREFILL_CANDIDATE` | legacy alias | compatibility | Historical opt-in alias. Packed8-q4 K prefill now auto-selects BM64 i8QK/PV-WMMA DBV whenever the K metadata is `packed8_q4_144` and the min-nq/min-nk guards pass. |
| `GGML_CUDA_ROCM_PACKED8_Q4_PWMMA_PREFILL` | legacy alias | compatibility | Short alias retained for old scripts; no longer required for normal packed8 prefill. |
| `GGML_CUDA_ROCM_PACKED8_Q4_PWMMA_PREFILL_DISABLE` | `0` | fallback/diagnostic | Disables the packed8-q4 PWMMA prefill auto route and leaves packed8 decode/verify on PDMQ. Use only for A/B or emergency fallback. |
| `GGML_CUDA_ROCM_PACKED8_Q4_PWMMA_PREFILL_MIN_NQ` | `16` | production guard | Minimum query rows for packed8 PWMMA prefill. Keeps verifier/small-Q batches on PDMQ. |
| `GGML_CUDA_ROCM_PACKED8_Q4_PWMMA_PREFILL_MIN_NK` | `512` | production guard | Minimum K rows for packed8 PWMMA prefill. Default matches 512-token prompt chunks. |
| `GGML_CUDA_ROCM_PACKED16_WMMA_DBV_KSHARED_PREFILL` | unset/`0` disables, `1` opts in for row packed16 K | default-off candidate / rollback | Routes pure packed16 PWMMA prefill to the BM64 i8QK/PV-WMMA DBV K-shared path. Stages each BN16 packed16-q8 K tile and scales in LDS before QK so the four Q waves reuse K payload/scales instead of reloading from global memory. Demoted back to explicit opt-in after the 2026-06-27 prefill marker regressed when noforce auto selected the kshared route; D16-planar/tile16 remains diagnostic and requires explicit `=1`. |
| `GGML_CUDA_ROCM_PACKED16_DBV_LEAN` | `0` | experimental opt-in | Uses the BM64 i8QK/PV-WMMA DBV production specialization with stream-K, live-shadow proof, profile, and QK-dump branches compiled out. Exactness contract is unchanged; default-off until hash and speed gates pass. |
| `GGML_CUDA_ROCM_PACKED16_DBV_SINGLE_VBUF` | `0` | experimental opt-in | Uses a BM64 i8QK/PV-WMMA DBV production specialization with the V tile staged in a single LDS buffer instead of the DBV double buffer, while also compiling out stream-K/proof/profile/QK-dump paths. Exactness contract is unchanged; default-off until hash and speed gates pass. |
| `GGML_CUDA_ROCM_PACKED16_DBV_GQA2` | `0` | experimental opt-in | Uses a DBV i8QK/PV-WMMA GQA2 specialization that maps two Q heads sharing one KV head into each CTA with 32 query rows per head. Exactness contract is unchanged; default-off until hash and speed gates pass. |
| `GGML_CUDA_ROCM_PACKED16_DBV_GQA6` | auto for Qwen3.6-27B-like V4/PV4, `0` optout | standard 27B prefill route | Uses the GQA6/BM96 DBV specialization (`PWMMA DBV GQA6_BM96`) by default for proven 24Q/4KV/GQA6 `V4_K16D16_144` row packed16 prefill. Set `0` to force the generic DBV family for rollback; set `1` to force the specialization for diagnostics on compatible shapes. |
| `GGML_CUDA_ROCM_PACKED16_DBV_NO_LOGITS_INIT` | `0` | experimental opt-in | Uses the single-V-buffer DBV production specialization and skips the per-K-tile logits zero-fill, with invalid/masked positions assigned before use. Exactness contract is unchanged; default-off until hash and speed gates pass. |
| `GGML_CUDA_ROCM_PACKED16_DBV_PROBS_OVERLAY` | `0` | experimental opt-in | Uses the single-V-buffer DBV production specialization and overlays the half-probability tile into row-local dead logits LDS after softmax, targeting lower LDS per CTA without changing probability precision. Exactness contract is unchanged; default-off until hash and speed gates pass. |
| `GGML_CUDA_ROCM_PACKED16_WMMA_BN32_PREFILL` | `0` | experimental opt-in | Routes pure packed16 PWMMA prefill to the BM64 i8QK/PV-WMMA BN32 candidate. This halves online-softmax update frequency versus BN16 DBV, supports V4_144 dispatch, and stays default-off until hash and speed gates pass. |
| `LLAMA_MTP_PACKED16_HOTCOLD_K` | `0` | deprecated experiment | Old hot/cold F16-K-shadow path. Ignored/fail-closed unless `LLAMA_MTP_PACKED16_HOTCOLD_K_EXPERIMENT=1` is also set. Current standard route uses packed16 PDMQ K with PV4/V144 V, not hot/cold. |
| `LLAMA_MTP_PACKED16_HOTCOLD_K_EXPERIMENT` | `0` | extra experiment opt-in | Required extra gate for the old hot/cold path. Keep unset for current PV4/V144 standard route work. |
| `LLAMA_MTP_PACKED16_HOTCOLD_K_WINDOW` | `256` | deprecated experiment scope | Maximum hot K window for the old hot/cold F16-K-shadow experiment. |
| `LLAMA_MTP_PACKED16_HOTCOLD_K_LEGACY_Q4V` | `0` | legacy opt-in | Allows old `V=q4_0` hot/cold eligibility, but only with `LLAMA_MTP_PACKED16_HOTCOLD_K_EXPERIMENT=1`. Keep unset for current PV4/V144 standard route work. |

## Legacy-prefix audit policy

Prefix registration is disabled. Existing legacy/debug families may remain in source only if their concrete env names appear as exact rows in this registry; otherwise `scripts/hip/qblock-env-audit.py` reports them as unregistered cleanup debt.

## Build gates

| CMake option | Default | Scope | Notes |
|---|---:|---|---|
| `GGML_HIP_PDMQ_COMPILE_LEGACY_Q4V` | `OFF` | PDMQ compile matrix | Compiles old `GGML_TYPE_Q4_0` V PDMQ debug/fallback cells. Keep OFF for the standard PV4/V144 build; enable only for legacy debugging/control builds. |
| `GGML_HIP_PDMQ_COMPILE_V4_144_PV4` | `ON` | PDMQ compile matrix | Compiles scalar PV4/V144 PDMQ cells for the standard PV4/V144 path. This is separate from the explicit DP16-DOT/profile switch and can be hard-disabled at runtime with `GGML_CUDA_ROCM_V4_K16D16_144_PV4_DISABLE=1`. |
| `GGML_HIP_COMPILE_STANDARD_Q8_FATTN` | `OFF` | HIP compile trim | Compiles legacy standard `GGML_TYPE_Q8_0` FlashAttention VEC instances (`q8_0/q8_0`, `q8_0/q4_0`). Keep OFF in the packed8-standard dev build; enable only for old q8 controls. |

## Removed / do-not-revive knobs

| Env | Status | Why |
| --- | --- | --- |
| `LLAMA_MTP_HEAD_CACHE_ONLY_NO_OUTPUT` | removed/do not revive | Unsafe cache-only/no-output shortcut. |
| `LLAMA_MTP_EXPERIMENTAL_HEAD_KV_PREFILL_ONLY` | default-off legacy experiment | Must not be used to claim strict correctness. |
| `LLAMA_MTP_EXPERIMENTAL_HEAD_OUTPUT_ROWS_ONLY` | default-off legacy experiment | Must not be used to claim strict correctness. |
| `GGML_CUDA_ROCM_Q8Q4_DOT4_PREFILL*` | removed q8q4 FA prototype | Old q8_0 K + q4_0 V DOT4 prefill route could auto-select in 2026-06-02 DP16/q8 logs and had no recent QBlock/V4 hits; prototype header and dispatch removed. |
| `GGML_CUDA_ROCM_Q8Q4_WMMA_I8*` | removed q8q4 FA prototype | Old q8_0 K + q4_0 V WMMA-I8 route had no searched runtime hits; prototype header and dispatch removed. |
| `GGML_HIP_LEGACY_Q8Q4_FA` / `GGML_CUDA_LEGACY_Q8Q4_FA` | removed temporary build gate | Superseded by deleting the q8q4 FA prototype headers and route plumbing. |
| `GGML_HIP_MMVQ_COMPILE_LEGACY_INTERLEAVED_ACT` / `GGML_CUDA_MMVQ_COMPILE_LEGACY_INTERLEAVED_ACT` | removed legacy MMVQ build gate | Superseded by deleting the non-K legacy interleaved activation route from `mmvq.cu`; generic MMVQ and K-quant interleaved routes remain. |
| `LLAMA_MTP_MMVQ_LEGACY_INTERLEAVED_ACT*` | removed legacy MMVQ route | Runtime knobs removed with the non-K legacy interleaved activation route. |
| `GGML_CUDA_PDMQ_WMMA_QK_CANDIDATE*` / `GGML_CUDA_ROCM_MTP_QBLOCK_WMMA_QK_CANDIDATE` / `LLAMA_MTP_QBLOCK_WMMA_QK_CANDIDATE` | removed PDMQ candidate path | Private consumed WMMA-QK marker/candidate path was product-runtime clutter without a current acceptance route. |
| `GGML_CUDA_PDMQ_DEBUG_KSCALE_ONE` / `GGML_CUDA_PDMQ_DEBUG_VSCALE_ONE` | removed wrong-result debug bypass | Wrong-result scale bypass experiments removed from product runtime. |
| `GGML_CUDA_PDMQ_DEBUG_SELF_PROBES` / `GGML_CUDA_PDMQ_DEBUG_SKIP_PROBES` | removed runtime self-probes | PDMQ synthetic self-probes removed from launch path; future probes belong in tests/microbenches. |
| `GGML_CUDA_PDMQ_PROFILE` | removed runtime profiler | Phase cycle profiling was intertwined with hot kernels and removed from the live route; use external profiling or a dedicated microbench. |
| `GGML_CUDA_PDMQ_WMMA_ORACLE_TRACE` / `GGML_CUDA_ROCM_MTP_QBLOCK_WMMA_ORACLE_TRACE` / `LLAMA_MTP_QBLOCK_WMMA_ORACLE_TRACE` | removed oracle trace | Host-side PDMQ WMMA layout telemetry removed from product runtime. |
| `GGML_CUDA_PDMQ_WMMA_QK_PARITY*` / `GGML_CUDA_ROCM_MTP_QBLOCK_WMMA_QK_PARITY` / `LLAMA_MTP_QBLOCK_WMMA_QK_PARITY` | removed parity probe | Device parity probe and printf state removed from live PDMQ kernels. |
| `GGML_CUDA_PDMQ_DEBUG_TAIL_PROB*` / `GGML_CUDA_PDMQ_DEBUG_STATE*` | removed debug probes | Tail probability, scalar-ref, input-probe, and state-print debug blocks removed from live PDMQ kernels. |
| `GGML_CUDA_ROCM_V4_K16D16_144_EXACT_SPLITK_CANDIDATE*` | removed tombstone | Demoted split-K candidate was only an aborting tombstone after scalar split-K failed speed and q4-LDS split-K diverged. |
| `GGML_HIP_PDMQ_FULL_EXPERIMENTAL_MATRIX` | removed build gate | Stage-2 cleanup made PDMQ compact-only; full experimental rebuild plumbing is not maintained. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_IMPL` | removed runtime selector | KSHARED/stagev/directv legacy variants were deleted from the live route surface. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_Q8V_N64` | removed q8v experiment | Q8V_N64 was a demoted N64-shape experiment and no longer has launcher/planner policy. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_SHAPE=m1n64/m2n64/m4n64` | removed shape values | N64 PDMQ shapes were deleted; compact shape overrides are `1x32`, `2x32`, `4x32`, `8x32`, `16x16`. |
| `GGML_CUDA_ROCM_PACKED16_DOT4_MMQ_VPATH=direct_pv_q4/direct_legacy` | removed VPATH values | Explicit direct legacy V aliases were deleted; compact VPATH remains `stage_f32`, raw LDS, and persistent V4 diagnostics. |

## Current known lesson

The old `WMMA_QK_CANDIDATE` marker path was removed rather than kept as a default-off runtime candidate. Future WMMA-QK work needs a fresh design note, likely logical/head-packed M fill, and must land as a tested implementation path rather than private product-runtime scaffolding.
